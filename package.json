{
  "name": "m1k3",
  "version": "1.0.0",
  "description": "M1K3 — privacy-focused local AI assistant (Python desktop CLI/MCP server + Swift macOS MVP + KMP mobile). Status badges live in README.md.",
  "main": "test-dashboard-monochrome.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
