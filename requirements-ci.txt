# Slim dependency set for the python-smoke CI job (.github/workflows/ci.yml).
# Deliberately excludes the heavy ML/audio stack (torch, transformers, vosk,
# sounddevice, pyaudio, whisper). The smoke subset in tests/ci_smoke.txt is
# curated to pass with ONLY these — keeping CI fast and reliable.
# Full local runs: pip install -r requirements.txt
pytest>=8.0
pytest-timeout>=2.3
duckdb>=1.0
numpy>=1.26.4,<2.0
pydantic>=2.0
