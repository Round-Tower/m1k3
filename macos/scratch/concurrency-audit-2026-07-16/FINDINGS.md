## [0] MLXEmbeddingService: unguarded check-then-act on modelContainer — double ~600MB model load + unsynchronized write race
**Sources/M1K3MLX/MLXEmbeddingService.swift:148** · dim=unchecked-sendable · claimed=high → verified=medium · effort=S

SCENARIO: warmEmbedderOnLaunch (AppEnvironment.swift:1354) kicks the ~4s cold load off the main actor at every cold launch. During that window, an in-app MCP request (MCPHostController.swift:260 embedQuery via the shared SwappableEmbeddingService), a first chat turn (AgentRAGResponder.swift:208), or the iOS MemoriesScreen calls ensureLoaded() concurrently, sees `modelContainer == nil`, and starts a SECOND EmbedderModelFactory.loadContainer — two ~600MB Metal-backed containers alive at once (memory spike on exactly the machines near the 16GB floor), plus two unsynchronized `modelContainer = container` writes to a plain var from different tasks — a bona fide data race (ARC retain/release on a shared reference, UB), not just wasted work. The class doc claims the MLX actor guards everything, but the container *property* is guarded by nothing.

FIX: Adopt the exact sibling pattern this repo already invented for this exact bug: single-flight the load through `SingleFlightLoader<EmbedderModelContainer>` (an actor, already in M1K3Inference and used by MLXGemmaProvider + both WhisperKit providers, whose 06-09 review note says it was added "to kill the same check-then-act double-download race") and cache the result behind an NSLock/Mutex for the sync reads. One file, no API change.

REVISED FIX: SingleFlightLoader yes, but drop the proposed NSLock/Mutex half — it's unnecessary. There are no sync reads of the container (dimension is a let; fingerprint reads only configuration). Delete the `modelContainer` var entirely: create `private let loader: SingleFlightLoader<EmbedderModelContainer>` in init, capturing `configuration` and constructing the factory call inside the operation closure (wire onLoadProgress via the loader's progress parameter, exactly MLXGemmaProvider.swift:104/212's shape). ensureLoaded() becomes `MLXMemoryBudget.applyOnce(); return try await loader.value { prog in report?(prog) }` — the completed Task IS the cache, instant for later callers, and failure clears the slot for retry (matching isAvailable()'s existing retry semantics). Also fix the now-stale @unchecked Sendable justification comment at lines 41-42 and add a MurphySig Review line documenting the change (the file is signed 2026-06-13 with a 07-09 review).

VERIFIER: Verified against the code: MLXEmbeddingService.ensureLoaded (Sources/M1K3MLX/MLXEmbeddingService.swift:148-162) is a genuine unguarded check-then-act on a plain `var modelContainer` in an @unchecked Sendable class — check at :152, long await on EmbedderModelFactory.shared.loadContainer at :154, unsynchronized write at :160. The upstream factory has NO cache or coalescing (verified in .build/checkouts/mlx-swift-lm/Libraries/MLXEmbedders/ModelFactory.swift and MLXLMCommon/ModelFactory.swift:174 — every loadContainer call loads fresh). Two concurrent paths are reachable: AppEnvironment.swift:603 

---

## [1] AppleSpeechTranscriber: stale recognition-task callback tears down the NEW listening session (no session generation)
**Sources/M1K3Voice/AppleSpeechTranscriber.swift:127** · dim=unchecked-sendable · claimed=medium → verified=medium · effort=S

SCENARIO: startListening() begins by calling stopListening(), which does `task?.cancel()` on session A. SFSpeechRecognizer delivers A's cancellation to the result handler as an error on its own queue, asynchronously. Meanwhile the new AsyncStream body has already stored continuation_B and begin() is installing session B's request/task. When A's cancel-error callback lands, `if error != nil { self.stopListening() }` runs against CURRENT state — it stops the engine, removes B's tap, cancels task_B and finishes continuation_B. Net effect: a listen restart (barge-in, voice-mode toggle, router flip) silently kills the fresh session; the user speaks and nothing is transcribed until they toggle the mic again. The lock guards each field access but nothing distinguishes WHICH session a callback belongs to.

FIX: Add a lock-guarded `generation: Int` bumped in startListening; capture the value into the result handler (and the route-change observer) and have stopListening take an `ifGeneration:` guard so a stale session's callbacks become no-ops instead of tearing down the current one. Same fix pattern as the WhisperKit sibling below — worth doing as one PR.

REVISED FIX: The proposed generation guard is correct; two refinements: (1) guard BOTH handler exits — the `error != nil` branch AND the `result.isFinal` branch — because stopListening calls request.endAudio() before task.cancel(), so a stale session can come back as a final result rather than a cancel error; (2) capture the generation once into the handler closure at recognitionTask creation (not by comparing self.task identity, which is assigned only after recognitionTask returns and can race an early error callback). Keep the generation check inside `lock` and engine teardown outside it, per the file's documented lock-inversion doctrine. Bundling with the WhisperKit sibling in one PR is sensible.

VERIFIER: Confirmed by reading the file and the live call path. The result handler (line 127-143) tears down CURRENT state on any error or isFinal with no session identity — task.cancel() in stopListening (line 91) reliably delivers an async 'canceled' error to the stale handler (and endAudio at line 90 can deliver a stale isFinal, hitting the same stopListening at line 142). The reachable concurrent path is VoiceLoopMachine.emptyListenEnded (VoiceLoopMachine.swift:134): [.stopListening, .startListening] executed back-to-back while session A is live; begin() for session B runs off-main (Task at AppleSpe

---

## [2] WhisperKitProvider: old streamer's error path and lastText writes leak into the next session (cross-session teardown + transcript pollution)
**Sources/M1K3WhisperKit/WhisperKitProvider.swift:156** · dim=unchecked-sendable · claimed=medium → verified=medium · effort=S

SCENARIO: stopListening() stops the old streamer in a detached `Task { await streamer?.stopStreamTranscription() }`, so old-session callbacks outlive the restart. Two concrete leaks into a new session started immediately after: (1) the old startStreamTranscription Task's catch calls `self.stopListening()` — which stops the NEW streamer, finishes the NEW continuation, and (worse) yields the shared `lastText` as a final segment; voice-first goes silently deaf mid-conversation. (2) the old streamer's onState callback writes the SHARED `self.lastText` under lock after the new session reset it to ""; the new session's stopListening then emits the PREVIOUS utterance as the new stream's `isFinal` segment — the user's prior sentence gets re-sent to the model as a fresh spoken turn (duplicate/ghost turn).

FIX: Session-scope the state: a lock-guarded generation counter captured by onState, the failure Task, and the final-segment emit; stale-generation callbacks return early. Move `lastText` into the per-session capture (it only exists to build the final segment) so it cannot be written by a zombie streamer.

REVISED FIX: The direction is right but incomplete, and the guard can be simpler than a generation counter. (a) Move lastText into a per-session lock-guarded box captured by onState and the final-segment emit — that alone kills the transcript-pollution leg. (b) Guard the failure Task with an identity check under the existing lock (`guard self.streamer === streamer else { return }` before calling stopListening) — simpler than a counter and equivalent here since the streamer reference IS the session. (c) CRITICALLY, also serialize teardown: both sessions' AudioStreamTranscribers wrap the SAME kit.audioProcessor, so the detached `Task { await streamer?.stopStreamTranscription() }` can call audioProcessor.stopRecording() AFTER the new session's startRecordingLive and silently kill the new session's mic (and a late-scheduled old start Task can pass `guard !state.isRecording` and re-open the mic after stop). Generation/identity scoping of callbacks does not fix that — the new session's start Task should first `await` the old streamer's stopStreamTranscription() (capture the old streamer in startListening and stop it at the head of the new session's Task) before calling startStreamTranscription on the new one.

VERIFIER: Verified against the file and the pinned WhisperKit source. The lastText-pollution leg is CONFIRMED: stopStreamTranscription is fire-and-forget (WhisperKitProvider.swift:171) and only flips the actor's isRecording flag — an in-flight transcribe keeps firing progress→onState callbacks that write the SHARED self.lastText (line 131) after the new session reset it (line 119); the new session's stopListening then emits the stale text as an isFinal segment (173-175). Reachable consumers: dictation auto-SENDS that final text to the model (AppEnvironment.swift:1789/1822 — duplicate/ghost turn), and th

---

## [3] EffectfulSpeechProvider: the four callback vars are unguarded on an @unchecked Sendable class — the exact hole its sibling AVSpeechProvider locked
**Sources/M1K3Voice/EffectfulSpeechProvider.swift:42** · dim=unchecked-sendable · claimed=low → verified=low · effort=S

SCENARIO: onSpeakingStarted/Ended/onTimelineReady/onWordSpoken are plain vars. They are written through SwappableSpeechProvider.applyCallbacks (called under its Mutex from whatever context calls setProvider or a callback setter — nonisolated public API) and read from @MainActor fire* helpers and the streaming session's closures. Today every production writer happens to be MainActor (AppEnvironment), so this is latent — but nothing enforces it, and AVSpeechProvider's 06-09 review locked the IDENTICAL properties for the IDENTICAL provider-swap reason ("a torn read across the @unchecked surface is now ruled out by an NSLock"). Any future off-main setProvider (e.g. the brain-at-home iOS shell wiring, or an MCP-driven voice-tier change) reintroduces an unsynchronized closure-ref race (torn ARC read → crash) with no compiler complaint.

FIX: Match the sibling: back the four vars with a lock (or Mutex<Callbacks> struct) exactly like AVSpeechProvider, OR mark them @MainActor (all current readers already are) so an off-main writer becomes a compile error. KokoroSpeechProvider forwards to these, so hardening this one seam covers both providers.

REVISED FIX: Use ONLY the lock variant — drop the @MainActor alternative entirely. The vars satisfy nonisolated protocol requirements (SpeechProviderWithLifecycle/SpeechProviderWithWordTiming, SpeechProvider.swift:79-100) and are assigned synchronously from SwappableSpeechProvider.applyCallbacks (nonisolated, inside its Mutex.withLock), so @MainActor on them is a compile-breaking change to the façade and the conformance. Back the four vars with a single Mutex<Callbacks> struct (Synchronization house style) or an NSLock to byte-match the AVSpeechProvider sibling (AVSpeechProvider.swift:28-62); readers fireSpeakingStarted/Ended and the streaming-session closures take the lock on get. KokoroSpeechProvider forwards to this renderer, so no change needed there.

VERIFIER: Every factual claim checks out on read: the four callback vars (EffectfulSpeechProvider.swift:42-47) are plain unguarded vars on an @unchecked Sendable class, which opts out of Swift 6 data-race checking; the sibling AVSpeechProvider locks the identical four properties with a signed 2026-06-09 review rationale naming the identical provider-swap write path; KokoroSpeechProvider forwards its setters to this renderer so one seam covers both. Writers flow through SwappableSpeechProvider.applyCallbacks under its Mutex, but that Mutex serializes façade callers only — not against EffectfulSpeechProvi

---

## [4] Modernization: SwappableEmbeddingService, RuntimeSelectionBox (and ToolCallRecorder) can drop @unchecked via Mutex — the pattern SwappableSpeechProvider already ships
**Sources/M1K3Knowledge/SwappableEmbeddingService.swift:26** · dim=unchecked-sendable · claimed=low → verified=low · effort=S

SCENARIO: No defect — escape-hatch removal with compiler-checked payoff. These three are textbook lock-around-one-slot types where NSLock forces @unchecked Sendable, so the compiler verifies nothing about future added state. SwappableSpeechProvider.swift:22 already proves the target shape in this codebase: `Mutex<State>` + plain `Sendable`, zero @unchecked. SwappableEmbeddingService (Mutex<any EmbeddingService>), RuntimeSelectionBox (Mutex<RuntimeOption> — or delete the class and inline the Mutex into RuntimeInferenceProvider), and ChatEvalStage.ToolCallRecorder (Mutex<[String]>) are all one-slot conversions; after each, any future unguarded property becomes a compile error instead of a review-time hope.

FIX: Convert each to Synchronization.Mutex holding the mutable slot and declare plain Sendable, mirroring SwappableSpeechProvider byte-for-byte in shape. Pure refactor, behavior-identical, existing tests (EmbedQuerySeamTests etc.) pin the seams. Do NOT touch the KVCache-holding types (PersonaPrefixCache, MLXToolTurnSession) — their comments correctly explain why Mutex can't hold non-Sendable KVCache.

REVISED FIX: Convert each to Synchronization.Mutex + plain Sendable, mirroring SwappableSpeechProvider. One correction: do NOT take the 'delete RuntimeSelectionBox and inline the Mutex into RuntimeInferenceProvider' alternative — the box is a shared handle written by AppEnvironment (AppEnvironment.swift:134, :511) and read by the provider; inlining would force AppEnvironment writes through the provider. Keep the box, put Mutex<RuntimeOption> inside it. Then also drop @unchecked from RuntimeInferenceProvider itself (line 38) — after the box conversion all its stored properties are let + Sendable, so plain Sendable checks. Add MurphySig Review lines to the signed files (SwappableEmbeddingService, RuntimeInferenceProvider) documenting the conversion. Leave PersonaPrefixCache and MLXToolTurnSession untouched as the auditor says.

VERIFIER: All three types verified in source exactly as claimed: SwappableEmbeddingService (Sources/M1K3Knowledge/SwappableEmbeddingService.swift:26-28, NSLock + @unchecked Sendable + one slot), RuntimeSelectionBox (M1K3App/RuntimeInferenceProvider.swift:22-34, NSLock around a String-enum RuntimeOption), ToolCallRecorder (M1K3App/ChatEvalStage.swift:55-65, NSLock around [String]). The house-pattern precedent is real and compiling: SwappableSpeechProvider.swift:22/33 uses Mutex<State> holding `any SpeechProvider` with plain Sendable under Swift 6 strict checking — and EmbeddingService/InferenceProvider a

---

## [5] send() lacks the re-entrancy guard its fire-and-forget callers assume — a second entry mid-stream silently drops the user's input and runs the whole post-turn block against the wrong turn
**M1K3App/AppEnvironment.swift:701** · dim=unstructured-tasks · claimed=high → verified=high · effort=S

SCENARIO: A turn is streaming (chat.isResponding == true). Two live entry points still fire `Task { await env.send(text) }`: (1) the composer mic — ContentView.swift:375 disables it only on `!env.canDictate && !env.isListening`, NOT on isResponding, so the user can dictate during a streaming answer and finishDictation (AppEnvironment.swift:1822) auto-sends; (2) follow-up chips on EARLIER complete messages (MessageView.swift:280 gates on the message's own .complete status, not on isResponding; wired at ContentView.swift:320). env.send passes `guard isReady`, then ChatSession.send's `guard !isResponding` (ChatSession.swift:271) returns WITHOUT appending — the user's dictated/tapped question vanishes silently. env.send then continues as if a turn completed: `surfaceCodeArtifact(from: chat.messages.last?.text)` opens the review panel with the PARTIAL text of the still-streaming answer, `stripArtifactTagsFromLastMessage()` mutates the streaming message, `evaluateBrainUpgradeAfterAnswer` records struggle signals from the wrong turn, and `avatar.resetToIdle()` drops the avatar to idle mid-generation. Worse, `applyAutoRouteIfEnabled()` runs FIRST and (auto-route on) can call selectBrain → `oldMLX.releaseMemory()` on the provider that is actively generating the in-flight turn.

FIX: Add `guard isReady, !chat.isResponding else { return }` at the top of AppEnvironment.send (before applyAutoRouteIfEnabled). For the dictation path, instead of dropping, stash the cleaned transcript into the composer draft (or await until isResponding clears) so the spoken input isn't lost. Optionally also disable the composer mic and follow-up chips on chat.isResponding for the honest-UI half.

REVISED FIX: The proposed fix is right in shape; two refinements. (1) Add `guard isReady, !chat.isResponding else { return }` at the top of AppEnvironment.send — before applyAutoRouteIfEnabled — matching the pattern MenuBarPopover.swift:277-281 and canSend (ContentView.swift:414-418) already use. (2) For dictation, a bare guard still silently discards spoken input, which is the worst outcome for this product: in finishDictation, when chat.isResponding, stash the cleaned transcript into the composer draft (needs a small seam — draft is @State in ContentView; either surface a pendingDictation property on AppEnvironment that ContentView folds into draft, or simply keep it in liveTranscript-style state) rather than dropping it. (3) Honest-UI half: add `|| env.chat.isResponding` to the mic button's disabled at ContentView.swift:390 (consistent with lines 371/534) and gate followUpChips (MessageView.swift:279-283) on an injected isResponding/interactionEnabled flag, since the message's own .complete status cannot see the session state. Do NOT touch ChatSession.send's guard — its silent no-op is documented and test-pinned; the funnel guard makes it unreachable from UI paths anyway.

VERIFIER: Every load-bearing claim verified against the code. (1) env.send (AppEnvironment.swift:695-701) guards only isReady — no isResponding. (2) Two live re-entrant entry points confirmed: the composer mic (ContentView.swift:390 disables only on !canDictate && !isListening; toggleDictation/startDictation have no isResponding guard; finishDictation:1808-1822 auto-sends with only isEmpty+isReady checks) and follow-up chips (MessageView.swift:280 gates on the message's OWN .complete status, so a prior answer's chips stay tappable while a new turn streams; wired unguarded at ContentView.swift:320). (3) 

---

## [6] startListening's unstructured begin() Task races stopListening — a stop that lands before begin finishes leaves the mic recording with no consumer and no teardown path
**Sources/M1K3Voice/AppleSpeechTranscriber.swift:67** · dim=unstructured-tasks · claimed=high → verified=high · effort=M

SCENARIO: startListening() spawns `Task { await self.begin(continuation) }` and returns the stream immediately. begin() suspends at `await Self.authorized()` — on first use that is the SYSTEM PERMISSION DIALOG, a window of many seconds. Sequence: user taps the mic → permission prompt appears → user taps the mic again to stop (or exits voice mode; VoiceLoopController's .stopListening command, stopDictation, and startListening's own leading stopListening() all reach stopListening()) → stopListening stops the (not-yet-started) engine, nils request/task/continuation and finishes the stream → user then grants permission in the still-open dialog → begin() resumes, writes a NEW request into the cleared state, installs the tap, starts a recognitionTask and `audioEngine.start()`. The mic is now live (orange privacy indicator ON in a privacy-first app), audio flows into an orphaned recognizer, yields go to a finished continuation, and NOTHING tears it down until the next startListening call or the recognizer happens to finalize. Same shape in WhisperKitProvider.startListening (WhisperKitProvider.swift:154): `Task { try await streamer.startStreamTranscription() }` can begin streaming after stopListening already ran `stopStreamTranscription()` on it.

FIX: Add a session generation counter (bumped under `lock` in stopListening). begin() captures the generation at spawn and re-checks it after EVERY suspension (post-authorized, and before installTap/engine.start): if stale, bail without touching engine state. Apply the same generation guard around WhisperKitProvider's startStreamTranscription Task. Both classes already own the lock needed to do this safely.

REVISED FIX: The generation-counter direction is right but the proposed "re-check after every suspension and bail" is insufficient alone: stopListening can still land between begin()'s last pre-check and `audioEngine.start()` (check→installTap→recognitionTask→start is not atomic and must not run under `lock` — the file's documented inversion). Complete shape: (a) generation counter bumped under `lock` as the FIRST thing stopListening does; begin() captures it at spawn and bails at each check point as proposed; (b) add a COMMIT-POINT epilogue — after `audioEngine.start()` succeeds, re-check the generation under `lock`; if stale, begin() runs the full teardown itself (endAudio/cancel/engine stop/removeTap/removeObserver), which is safe because it holds the only references. For WhisperKitProvider: guard the start Task with `lock.withLock { self.streamer === streamer }` before calling startStreamTranscription (stopListening nils the field synchronously, so a prior stop is always visible), and because actor-call ordering with stopListening's unstructured stop Task is still unguaranteed, add a belt: if the identity check turns stale after startStreamTranscription is invoked, call stopStreamTranscription on the captured streamer again. Both fixes are verify-by-launch (TCC prompt + real mic) per the file's own convention — state that in the PR rather than claiming swift-test proof.

VERIFIER: Verified against the actual code and callers; I tried to refute it on three grounds and all three fail.

(1) "Is one side serialized?" No. All *callers* of start/stopListening are @MainActor (VoiceLoopController.Dependencies closures are declared @MainActor; AppEnvironment.startDictation/stopDictation are MainActor), but `Task { await self.begin(continuation) }` at AppleSpeechTranscriber.swift:67 is spawned from a non-isolated method of a non-actor class — Package.swift sets no defaultIsolation, so begin() runs on the global executor, fully concurrent with any MainActor stopListening. The clas

---

## [7] Untracked duplicate preloadGemma() runs defeat the brain-switch cancellation — a superseded launch warm writes a stale modelLoad = .ready mid-download and re-loads released weights via the persona warm
**M1K3App/AppEnvironment.swift:956** · dim=unstructured-tasks · claimed=high → verified=medium · effort=S

SCENARIO: The cancellation design only covers the didSet-tracked `preloadTask` (line 355-366). But warmUpSelectedBrainOnLaunch (line 1299) is called from BOTH M1K3App.swift:231 and ContentView.swift:138 and each awaits `preloadGemma()` directly — up to three concurrent untracked frames at launch. Scenario: launch restores Big (6.7GB, still loading), user picks Lil in the picker/Settings → selectBrain cancels only preloadTask and starts Lil's warm. The untracked Big frame keeps running (SingleFlightLoader deliberately finishes the load); when it completes its ONLY guard is `if Task.isCancelled` — false — so it (a) writes `modelLoad = .ready` — if Lil is still downloading, the readiness gate opens early (the exact 'interacted before ready' bug class this gate exists to stop), the download UI vanishes, and all of Lil's later progress hops are dropped by the `if case .downloading` guard; and (b) calls `warmPersonaPrefixAfterLoad(mlx)` with the OLD provider — whose weights selectBrain just `releaseMemory()`d — and warmPersonaPrefix re-loads the multi-GB container for a brain that is no longer selected (a silent ~7GB RAM spike on a 16GB-floor machine).

FIX: Make preloadGemma supersession-aware instead of cancellation-only: before the `.ready` write (and in the catch), `guard mlx === currentMLXProvider else { return }` — one identity check covers every caller. Additionally route warmUpSelectedBrainOnLaunch through the tracked task (`preloadTask = Task { await preloadGemma() }; await preloadTask?.value`) so a brain switch cancels the launch warm like any other.

REVISED FIX: Adopt the identity-guard half of the proposed fix, extended: in preloadGemma, capture `let mlx = currentMLXProvider` (already done, line 936) and guard `mlx === currentMLXProvider` (a) inside the progress closure's MainActor hop — otherwise a superseded frame's fractions clobber the new brain's bar while it is .downloading, (b) before `modelLoad = .ready` + `warmPersonaPrefixAfterLoad(mlx)`, and (c) in the generic catch before `modelLoad = .failed`. Document the switch-to-Mini edge: selectBrain's AFM branch does not reassign currentMLXProvider (904–905), so a stale frame passes the guard there — benign (Mini's readiness ignores modelLoad, and .ready for the still-current, genuinely loaded provider makes reselectIsNoOp correct) but should be a comment, not an accident. Drop or rework the second half of the proposed fix: naively routing warmUpSelectedBrainOnLaunch through `preloadTask = Task {...}` lets the two launch callers (app delegate + ContentView .task) stomp each other's task reference, recreating an untracked frame; either await an existing in-flight preloadTask instead of creating a second, or skip the routing entirely — the identity guard alone covers every caller. Also update preloadGemma's now-false header comment (930–934) to name supersession-by-identity rather than cancellation as the clobber defence.

VERIFIER: Verified in source. (1) Untracked frames: warmUpSelectedBrainOnLaunch (AppEnvironment.swift:1299) awaits preloadGemma() directly from M1K3App.swift:231 (never-cancelled Task), ContentView.swift:146 (.task) and the ModelGateView retry closure (ContentView.swift:138); init assigns selectedRuntime directly so the didSet never runs and preloadTask is nil at launch — selectBrain's preloadTask?.cancel() (line 355) cancels nothing. (2) preloadGemma's only stale-defence is `if Task.isCancelled` (955), which is false for these frames; releaseMemory() (MLXGemmaProvider.swift:272) does not cancel the Sin

---

## [8] setEnabled spawns unordered start/stop Tasks — a fast double-toggle can leave the loopback MCP server running while the toggle reads OFF (or dead while ON)
**M1K3App/MCPHostController.swift:103** · dim=unstructured-tasks · claimed=medium → verified=medium · effort=S

SCENARIO: setEnabled fires `Task { enabled ? await start() : await stop() }` with no serialisation between successive toggles. Direction A (privacy-relevant): user toggles ON then OFF while `host.start()` is still awaiting the NWListener bind + `makeSession()` — the stop Task runs first, sees `server == nil`, no-ops and clears state; start() then completes and assigns `server = host; isRunning = true; statusText = "Running…"`. Final state: isEnabled == false and the persisted default says OFF, but the MCP server keeps serving on 127.0.0.1:4242 until the next full toggle cycle or quit. Direction B: OFF→ON while stop() is suspended at `await server?.stop()` — start()'s `guard server == nil` still sees the old server and returns; stop then finishes clearing — toggle shows ON, server permanently down. Both are silent (Settings renders the stale statusText/isRunning).

FIX: Serialise the lifecycle: store `private var lifecycleTask: Task<Void, Never>?` and chain — `lifecycleTask = Task { [prev = lifecycleTask] in await prev?.value; enabled ? await self.start() : await self.stop() }`. In start(), after `host.start()` succeeds, re-check `isEnabled` and stop the host immediately if the user disabled meanwhile, so the final state always converges on the toggle.

REVISED FIX: The proposed fix is correct; extend it to cover the third unordered spawn: startIfEnabled() (line 96) must route through the same lifecycleTask chain, since a launch-time start racing an early user toggle-OFF hits the identical Direction A. Concretely: `private var lifecycleTask: Task<Void, Never>?`; both startIfEnabled and setEnabled do `lifecycleTask = Task { [prev = lifecycleTask] in await prev?.value; await self.applyDesiredState() }` where applyDesiredState reads self.isEnabled and starts/stops accordingly (reading the CURRENT desire rather than the captured parameter makes the post-start convergence re-check largely automatic, though keeping the explicit re-check after host.start() as belt-and-braces is fine). Optionally also key the MenuBarPopover BarToggle on isEnabled rather than isRunning so the control reflects user intent during the async window.

VERIFIER: Verified against the actual code. MCPHostController is @MainActor but actor reentrancy lets successive setEnabled Tasks interleave at awaits. start() has a real multi-suspension window between `guard server == nil` (MCPHostController.swift:106) and `server = host` (line 140): `host.start()` awaits an NWListener bind continuation (LocalMCPHTTPServer.swift:98-125) plus `makeSession()` (server build + transport start). stop() likewise suspends at `await server?.stop()` (line 151) before clearing state. Direction A (ON→OFF during bind): the stop task no-ops on nil server, then the start task resum

---

## [9] synthesizeToFloats continuation never resumes if the offline render is cancelled or errors — SynthBox has no didCancel/didFinish/error backstop
**Sources/M1K3Voice/EffectfulSpeechProvider.swift:229** · dim=continuations-and-streams · claimed=high → verified=medium · effort=S

SCENARIO: SynthBox resumes the withCheckedThrowingContinuation ONLY when AVSpeechSynthesizer.write delivers the final zero-frame buffer. stop() unconditionally calls synthesizer.stopSpeaking(at: .immediate) (line 159), and isSpeaking() is false during the synthesis window (speakingState not begun, no streamingSession), so a barge-in/stop that lands while write is mid-render kills the render without the empty-buffer sentinel ever arriving — SynthBox.finish() never runs, the continuation leaks, and the speak() task suspends forever. Callers that await speak() hang: the MCP speak tool's HTTP call never returns, and a voice-loop speakTask on the Built-in tier is stuck until the user interrupts (onSpeakingStarted/Ended never fire, so speechDidEnd never dispatches). Same never-resume if write fails outright (missing voice asset) — no error path exists at all.

FIX: Implement speechSynthesizer(_:didCancel:) and speechSynthesizer(_:didFinish:) on SynthBox, routing both through the existing done-flag finish() (didCancel resumes with what was accumulated or a CancellationError). The done flag already makes a late empty-buffer callback a no-op, so the exactly-once guarantee is preserved. Optionally have stop() resume any in-flight synthesis continuation the way it already resumes playbackContinuation.

REVISED FIX: Implement speechSynthesizer(_:didFinish:) and speechSynthesizer(_:didCancel:) on SynthBox, but do NOT resume didCancel with the accumulated samples or a generic error — both are wrong for this file's control flow: resuming with partial samples makes speak(_:) proceed into speak(stream:.single(...)) and PLAY the audio the user just stopped, and resuming with a generic error hits speak(_:)'s catch which calls plainSpeak(utterance), re-speaking the whole cancelled utterance in the plain voice. Instead: didFinish routes through the existing done-flag finish() as an idempotent backstop (success with accumulated payload — the sentinel normally beat it there, done-flag makes it a no-op); didCancel resumes exactly once with CancellationError() via the same done-flag guard; and speak(_ utterance:) gains `catch is CancellationError { return }` before the plainSpeak fallback so a barge-in ends silently (cancellation is not a failure — consistent with the header's fallback-never-silent doctrine, which is about failures). Keep the existing exactly-once done-flag design untouched; no change to stop() needed since the didCancel delegate fires synchronously from stopSpeaking.

VERIFIER: Mechanically the claim is fully verified: SynthBox resumes the withCheckedThrowingContinuation only from ingest() on the zero-frame sentinel (EffectfulSpeechProvider.swift:360-395); it implements no didCancel/didFinish and no error route; stop() unconditionally calls synthesizer.stopSpeaking(at:.immediate) (line 159); and during synthesizeToFloats the cached speakingState is un-begun and streamingSession is nil, so isSpeaking() is false and stop()'s wasActive/hadPlayback bookkeeping resumes nothing. The file's own line-57 comment proves the author knows this exact AVFoundation class of hang on

---

## [10] speak entry guard is check-then-act across a suspension — two concurrent speaks interleave onto one player and clobber the synth delegate
**Sources/M1K3Voice/EffectfulSpeechProvider+Streaming.swift:80** · dim=continuations-and-streams · claimed=medium → verified=medium · effort=S

SCENARIO: speak(stream:) begins `if await isSpeaking() { await stop() }`; isSpeaking() hops through MainActor.run, a suspension point. Two concurrent speaks (e.g. the MCP speak tool firing while a voice-loop answer starts, both reaching the same EffectfulSpeechProvider) can both read false before either assigns streamingSession, then both proceed synchronously: B overwrites streamingSession, and BOTH sessions schedule buffers onto the single shared AVAudioPlayerNode — interleaved garbled audio, plus double onSpeakingStarted/Ended, and session A is never cancelled (stop() can only reach B). The speak(_:) path has the same window one stage earlier: during synthesis isSpeaking() is false for everyone, so a second speak reassigns `synthesizer.delegate = box` (EffectfulSpeechProvider.swift:228) while utterance 1's write is in flight — utterance 1's willSpeakRange onsets land in box 2 against the wrong text and sample counts, corrupting both word timelines (karaoke highlight tracks garbage).

FIX: Make speak admission atomic on the main actor: a MainActor `speakEpoch`/in-flight token bumped synchronously at entry; after the `await stop()` (and after synthesizeToFloats returns) re-check the token and bail if superseded. Assign streamingSession before any suspension and treat an existing session (even pre-first-buffer) as "speaking" for admission — the isSpeaking() cache already half-does this; the gap is the re-check after resuming from the await.

REVISED FIX: The proposed fix is essentially right; two refinements. (1) In speak(stream:) (@MainActor): bump a MainActor speakEpoch synchronously at entry, capture it, and — instead of only bailing when superseded — synchronously cancel any existing streamingSession at entry (session.cancel() is synchronous MainActor and already resumes its waiter; the deferred node.stop() inside complete() keeps the priority-inversion avoidance intact), then re-check the captured epoch after every resume point (after `await stop()`/`await isSpeaking()`) and bail if superseded so the loser never creates a session. This guarantees the superseded session is always cancelled rather than orphaned, closing the awaitCompletion-forever hang. (2) In speak(_:): re-check the epoch after synthesizeToFloats returns and drop the rendered PCM if superseded; that covers the delegate-clobber window without touching the SynthBox design (the wide window there is only reachable via Kokoro's fallback path, so no synthesizer-level serialization is needed). Keep isSpeaking()'s streamingActive term unchanged — with atomic admission it becomes belt-and-braces.

VERIFIER: CONFIRMED with one scope correction. (1) Suspension points are real: speak(stream:) is @MainActor but isSpeaking() is a nonisolated async func (hops off-actor under this package's settings — no NonisolatedNonsendingByDefault in Package.swift), and the `await stop()` branch is a longer suspension with no re-check after resuming. Multiple main-actor job boundaries allow two speak tasks to both read isSpeaking()==false before either assigns streamingSession. (2) Concurrency is reachable and admitted in-code: EffectfulSpeechProvider.swift:107-109 states "AppEnvironment.speak() does not serialise c

---

## [11] Entire call buffered in RAM: nearSamples/farSamples grow unbounded (~1.4 GB/hour) until stop()
**Sources/M1K3Calls/StereoCallRecorder.swift:57** · dim=continuations-and-streams · claimed=medium → verified=medium · effort=M

SCENARIO: Both the mic tap (48 kHz mono Float32, appended per 4096-frame buffer) and the SCStream audio callback append every sample to in-memory arrays for the whole recording: ~192 KB/s per channel ≈ 1.38 GB/hour for stereo. A 90-minute call is ~2 GB of Float arrays (with repeated grow-realloc copies under the lock on the audio callback path), on a machine that may simultaneously hold a 6.7 GB gemma-4-12B in Metal memory. Memory pressure → MLX eviction/thermal shed at best, watchdog/jetsam of the app at worst — and a killed app loses the ENTIRE recording since nothing is on disk until stop() writes the file.

FIX: Stream to disk incrementally: write each converted buffer through CallAudioWriter/AVAudioFile into per-channel .partial files (or one stereo file with a ring for channel alignment) as they arrive, keeping only small staging buffers in memory; finalize/validate/rename at stop(). Also bounds crash-loss to the last flush interval instead of the whole call.

REVISED FIX: Directionally right but refine for compatibility with the signed 2026-06-21 CallAudioWriter invariants (non-interleaved processingFormat, .partial → frame-validated → atomic rename, no orphans) and the existing t=0 index alignment + tail-padding semantics: (1) a periodic drain task — NOT the audio callbacks; no file I/O under the NSLock or on the SCStream captureQueue — swaps the staged arrays out under the lock and appends each channel to its own mono .partial AVAudioFile; (2) stop() performs a bounded-memory chunked merge of the two mono files into the final stereo .caf through the existing validate-frames + atomic-rename gate, silence-padding the shorter channel at the tail exactly as today; (3) crash recovery can optionally salvage the per-channel .partial files (bounding loss to the last flush). Avoid the proposed 'one stereo file with a ring for channel alignment' — it adds an aligner the current semantics don't need and risks changing pinned padding behaviour. Keep the CallAudioWriter.write(near:far:) array API for the merge/format logic so the existing unit tests keep their contract; capture side stays verify-by-launch per house convention.

VERIFIER: Confirmed by reading StereoCallRecorder.swift, CallAudioWriter.swift, and the AppEnvironment call path. Both audio callbacks (mic tap :172, SCStream :288) append every sample to member arrays drained only in stop(); no duration cap exists anywhere. ~1.38 GB/hour is arithmetically correct (48kHz Float32 × 2 ch). The claim actually UNDERSTATES the peak: CallAudioWriter.write allocates a second full-length stereo AVAudioPCMBuffer at stop() while the source arrays are still live, so a 1-hour call peaks ~2.8 GB — one contiguous allocation that can itself fail on a 16GB Mac holding the 6.7GB gemma-4

---

## [12] FileDownloader stages HTTP error bodies as model weights — a 404/503 permanently bricks the neural voice; download also ignores task cancellation
**Sources/M1K3Kokoro/KokoroSpeechProvider.swift:274** · dim=continuations-and-streams · claimed=medium → verified=medium · effort=S

SCENARIO: urlSession(_:downloadTask:didFinishDownloadingTo:) moves the temp file into place and resumes success without ever checking the HTTP status. If GitHub returns 404/503/HTML (release asset moved, captive portal), the error body is saved as kokoro-v1.0.onnx, `_ready` flips true, and isModelStaged returns true on every future launch — prepare() short-circuits forever, ORTSession init fails, and every utterance silently falls back to the Apple voice. The user paid for a 326 MB "successful" download and the M1K3 Voice tier is dead until they manually reset the container. Separately, run()'s withCheckedThrowingContinuation has no cancellation handler: cancelling the preparing task (user closes onboarding) leaves the 326 MB download running and the awaiting task suspended until it completes.

FIX: In didFinishDownloadingTo, check (downloadTask.response as? HTTPURLResponse)?.statusCode == 200 before moving the file; resume(throwing:) otherwise (the done-nil pattern already prevents double-resume with didCompleteWithError). Wrap run() in withTaskCancellationHandler that cancels the URLSession task. Consider validating staged files (size floor or magic bytes) in isModelStaged so a bad stage can self-heal.

REVISED FIX: Keep the proposed shape with three refinements. (1) In didFinishDownloadingTo, capture the status from downloadTask.response as? HTTPURLResponse and require (200..<300).contains(statusCode) — not == 200 — before moving the file; resume(throwing:) a descriptive error otherwise (the existing resume-then-nil pattern keeps the exactly-once invariant on the serial delegate queue; didCompleteWithError sees nil continuation and skips). (2) The higher-value half: add a size-floor validation to isModelStaged AND prepare()'s files-exist fast path (e.g., kokoro-v1.0.onnx > 50 MB, voices-v1.0.bin > 1 MB) — this both catches the status-check-proof failure class (captive portal returning 200 + HTML) and SELF-HEALS installs already bricked in the field by deleting the undersized file and re-downloading; a status check alone leaves existing bad stages permanent. (3) The cancellation handler (withTaskCancellationHandler cancelling the download task) is worthwhile hardening to bundle in the same S-effort PR, but describe it honestly: no current call site cancels the preparing task, so it fixes a latent trap, not a live hang. Do NOT change the swallowed-preload-failure design (Apple-voice fallback is a signed deliberate recovery); the fix belongs upstream at the download/staging boundary.

VERIFIER: The primary defect is confirmed by direct code read. FileDownloader.didFinishDownloadingTo (KokoroSpeechProvider.swift:271-288) moves the downloaded file and resumes success without inspecting the HTTP response; URLSession treats a 404/503 as a completed download (didCompleteWithError gets nil), so an error body gets staged as kokoro-v1.0.onnx. The permanence chain is fully reachable and verified in callers: prepare() equates file-existence with staged (line 168), swallows synthesizer.preload() failure by design (warning only, lines 174-177/199-205), and prepareM1K3Voice (AppEnvironment.swift:

---

## [13] stopListening fire-and-forgets stopStreamTranscription on the SHARED audioProcessor — a rapid re-listen can be killed deaf by the previous session's teardown
**Sources/M1K3WhisperKit/WhisperKitProvider.swift:171** · dim=continuations-and-streams · claimed=medium → verified=medium · effort=S

SCENARIO: Every AudioStreamTranscriber is built over the same kit.audioProcessor. stopListening() launches `Task { await streamer?.stopStreamTranscription() }` and returns immediately; startListening() (which itself calls stopListening() first) then constructs and starts a NEW streamer. If the detached teardown of the old streamer runs after the new one's startStreamTranscription begins capturing, its audioProcessor.stopRecording() halts the new session's shared capture: the new stream yields nothing, voice mode sees zero segments, the silence endpointer fires an empty utterance, and the listen cycle is silently wasted (mute→unmute or a quick re-dictation is the trigger). There is no ordering barrier between old-stop and new-start.

FIX: Make teardown awaitable and sequence it: store the stop Task and have startListening await it before constructing the new streamer (or make stopListening async and await stopStreamTranscription directly — callers already tolerate async via the Dependencies seam). Alternatively generation-stamp sessions and have the teardown skip audioProcessor.stopRecording() when a newer session is live.

REVISED FIX: Use only the auditor's first option: store the teardown Task in the lock-guarded state (e.g. `private var stopTask: Task<Void, Never>?`), and in startListening's streamer-start Task do `await stopTask?.value` (read under lock) BEFORE `startStreamTranscription()`. Single file, seam untouched. Reject the other two proposals: (a) making stopListening async is not free — VoiceLoopController.Dependencies.stopListening is a synchronous @MainActor () -> Void closure, so the protocol change ripples through both providers and the app shells; (b) generation-stamping to skip audioProcessor.stopRecording() when a newer session is live LEAKS a running AVAudioEngine with live taps, because startRecordingLive overwrites audioEngine without stopping the previous one.

VERIFIER: Verified against WhisperKit's checked-out source: stopStreamTranscription() unconditionally calls audioProcessor.stopRecording() on the SHARED kit.audioProcessor, and AudioProcessor.stopRecording() tears down whatever audioEngine is currently installed (removeTap, disconnect, stop, reset, nil). WhisperKitProvider.stopListening() fire-and-forgets that teardown in an unstructured Task (line 171), and startListening() — which calls stopListening() first — launches the new streamer's startStreamTranscription in a second unstructured Task on a DIFFERENT actor instance. Nothing orders them: the prov

---

## [14] Transcriber AsyncStreams have no onTermination — a cancelled consumer leaves the mic tap, engine, and recognition task running
**Sources/M1K3Voice/AppleSpeechTranscriber.swift:65** · dim=continuations-and-streams · claimed=low → verified=low · effort=S

SCENARIO: startListening()'s AsyncStream relies entirely on the consumer remembering to call stopListening(): if the consuming task is cancelled (iteration just ends) without that paired call, the AVAudioEngine keeps the mic hot, the tap keeps appending to the recognition request, and the SFSpeechRecognitionTask keeps running — an invisible live microphone (the exact privacy class this app hardens against). Today's two consumers (VoiceLoopController pairs cancel+stopListening in one command; MCPHostController's watchdog calls stopListening before its drain-deadline cancel) happen to pair correctly, so this is latent — but the invariant lives in every call site instead of the producer. WhisperKitProvider.startListening (WhisperKitProvider.swift:116) has the identical shape.

FIX: In both providers set `continuation.onTermination = { [weak self] _ in self?.stopListening() }` inside the AsyncStream builder, making mic teardown structural on consumer cancellation/deallocation. stopListening is already idempotent (nil-swap under lock; finish-after-finish is a no-op), so the double-call from explicit stops is safe.

REVISED FIX: Gate the handler on the termination reason instead of firing unconditionally: `continuation.onTermination = { [weak self] reason in if case .cancelled = reason { self?.stopListening() } }` in both providers. `.finished` only ever originates from stopListening itself (or begin's pre-start failure paths where nothing is armed), so cancellation is the only case needing structural teardown — this sidesteps WhisperKitProvider's re-entrancy entirely (its stopListening finishes the continuation before nilling streamer/continuation, so the unconditional form would double-call stopStreamTranscription on the same AudioStreamTranscriber). Alternatively/additionally, reorder WhisperKitProvider.stopListening to nil state under lock before finish(), matching AppleSpeechTranscriber's order. Same-PR bonus (pre-existing gap the proposed fix doesn't cover): AppleSpeechTranscriber.begin runs in a detached Task and never re-checks that its continuation is still current — a stop landing before begin executes lets begin re-arm the engine against a dead stream; add a `lock.withLock { self.continuation === ... }`-style identity guard (or check self.request is still the one it stored) before starting the engine.

VERIFIER: Verified against the actual code: both AppleSpeechTranscriber.startListening (line 63-69) and WhisperKitProvider.startListening (line 116-164) build AsyncStreams with no onTermination; all mic/engine/recognition teardown lives solely in stopListening(). Attempted refutation by auditing all three consumers: AppEnvironment.startDictation (the isListening guard + provider-internal stopListening make its cancel purely belt-and-suspenders), VoiceLoopController (.stopListening command pairs cancel+stopListening), and MCPHostController.listenForTranscript (watchdog stops at `timeout`, drain-deadline 

---

## [15] Connection tasks are untracked and unbounded — stop() never cancels them, and a silent client pins a suspended receive forever
**Sources/M1K3MCPKit/LocalMCPHTTPServer.swift:149** · dim=continuations-and-streams · claimed=low → verified=low · effort=S

SCENARIO: handle() re-checks `isRunning` only after receiveChunk returns, and receiveChunk's continuation resumes only when data, EOF, or an error arrives. A local client that opens a connection and sends nothing (or half a request) leaves the actor task suspended in connection.receive indefinitely — there is no per-connection deadline. stop() cancels the listener but never touches live connections, so those suspended tasks and their NWConnections outlive the "stopped" server (and survive a start/stop/start cycle, holding stale sockets). Loopback-only limits the attacker story, but a wedged MCP client accumulates one leaked task + connection per retry.

FIX: Track open connections in the actor (Set keyed by ObjectIdentifier, removed in handle()'s exit path); stop() cancels each — connection.cancel() fails the pending receive, resuming the continuation with nil and unwinding the task. Add a per-connection idle deadline (e.g. race receiveChunk against a 30 s Task.sleep, or NWConnection's send/receive timeout) so a half-open request can't pin a task.

REVISED FIX: Three parts, all in LocalMCPHTTPServer.swift: (1) Track live connections in actor state (`var connections: [ObjectIdentifier: NWConnection]`), inserted at the top of handle(), removed in its exit path; stop() cancels each — cancel fails the pending receive, resuming receiveChunk's continuation nil and unwinding the task. (2) Guard the post-receive loop body: after `await receiveChunk` returns, re-check `isRunning` before respond() (and/or guard isRunning inside respond()'s initialize branch) so a request landing on a held connection after stop() gets dropped, not served by a freshly-built zombie session — this is the off-means-off fix. (3) Idle deadline: do NOT race the receiveChunk continuation against Task.sleep (an abandoned checked continuation is runtime misuse); instead start a per-connection watchdog Task that sleeps ~30s and calls connection.cancel() unless a request completed — cancellation fires the receive completion with an error, which already maps to nil. Optionally also fix start()'s `session = try await makeSession()` to stop any existing session first (defence-in-depth against the zombie).

VERIFIER: Verified against LocalMCPHTTPServer.swift and its only caller (MCPHostController). The claim's mechanics all hold: (1) receiveChunk (line 196) wraps NWConnection.receive in a checked continuation that resumes only on data/EOF/error — a live-but-silent local client (e.g. an HTTP client holding a pooled/keep-alive connection) pins the handle() task suspended indefinitely; NWParameters.tcp sets no idle timeout and TCP has none by default. (2) stop() (line 136) cancels the listener and tears down the session but never touches accepted connections — NWListener.cancel() does not cancel already-accep

---

## [16] begin() does not re-check session liveness — a stopListening racing the startup Task leaves the mic engine hot after stop
**Sources/M1K3Voice/AppleSpeechTranscriber.swift:114** · dim=locks-and-queues · claimed=high → verified=high · effort=S

SCENARIO: startListening() stores the continuation and spawns Task { begin(continuation) }. If stopListening() runs before begin() executes (voice-mode teardown racing startup, a user cancelling immediately, or the isFinal-path stop racing a restart), stopListening finds request/task/continuation nil-or-stale and returns — then begin() proceeds anyway: it re-stores self.request (line 114), installs the mic tap, starts audioEngine, and creates a recognition task, all yielding into an already-finished continuation. The caller believes listening is stopped, but the mic is capturing and the engine is running indefinitely (until a chance isFinal fires) — the mic-in-use indicator stays lit in a privacy-first app with no UI way to turn it off.

FIX: Add a session epoch: increment a lock-guarded generation counter in startListening/stopListening; begin() captures its generation and re-checks it under the lock immediately before storing the request and again after engine.start() — if stale, tear down (removeTap, engine.stop, finish) and bail. Alternatively check identity `lock.withLock { self.continuation != nil }` at both points since stopListening nils it.

REVISED FIX: Keep the epoch/generation-counter fix but DROP the proposed "alternatively check `lock.withLock { self.continuation != nil }`" fallback — it is incorrect: in the isFinal-vs-restart race a new startListening() has already stored a new non-nil continuation, so a stale begin passes the nil check and then clobbers the live session's request/task and double-installs the tap (NSException). AsyncStream.Continuation is not Equatable, so identity comparison is unavailable; the counter is the only correct shape. Concretely: a lock-guarded `var generation: UInt64`; startListening and stopListening each increment it under `lock`; begin(continuation:generation:) receives its generation and (a) re-checks under `lock` immediately after `await Self.authorized()` and BEFORE storing self.request — if stale, just `continuation.finish()` and return without touching the engine (never tear down: a newer session may own it); (b) re-checks after `audioEngine.start()` — if stale, remove only the tap it installed and stop the engine under `engineLock`, then finish. Additionally move begin()'s installInputTap() + prepare/start under `engineLock` — they currently bypass the file's own documented engine-serialization between stopListening and the route-change handler. Effort S, single file, verify-by-launch (TCC-dialog repro above) per house convention.

VERIFIER: CONFIRMED by reading AppleSpeechTranscriber.swift and all three live callers (AppEnvironment+VoiceMode.swift:262-305 via VoiceLoopController, AppEnvironment.swift:1782/1805 dictation, MCPHostController.swift:342/360 listen tool). The race is reachable: startListening() is nonisolated, so Task { begin() } runs on the global executor concurrent with the @MainActor stopListening closures; begin() suspends at `await Self.authorized()` — on first run that is the TCC dialog, a seconds-to-minutes window. Reproduction: first voice use → TCC dialog → user exits voice mode (.stopListening: request/task 

---

## [17] Waiter cancellation is a no-op: `try await task.value` never throws on waiter cancel, so the CancellationError branch is unreachable and cancelled callers stay suspended for the whole load
**Sources/M1K3Inference/SingleFlightLoader.swift:64** · dim=locks-and-queues · claimed=medium → verified=medium · effort=M

SCENARIO: Verified experimentally (compiled probe): cancelling a task that is awaiting an unstructured Task's `.value` does NOT interrupt the wait — it blocks until the underlying task completes, then returns the value. Concrete path: MLXGemmaProvider.generateStreaming's onTermination cancels the generation task when the user hits Stop during a first-turn model load; that task is parked in ensureLoaded → loader.value → task.value and stays suspended for the entire multi-GB load (minutes on a fresh Big download), then briefly spins up a generation session on a cancelled task before dying. Same for any cancelled prepareModel caller. The file's documented contract ("if the calling task is cancelled, await task.value throws CancellationError") and the `catch let error as CancellationError` keep-the-slot branch are dead code — the PR #10 review behaviour it describes does not exist.

FIX: Make the wait cancellation-responsive: keep the shared load Task, but have waiters register a CheckedContinuation with the actor (resumed by a completion hook on the load Task) wrapped in withTaskCancellationHandler so onCancel resumes that waiter with CancellationError while the load continues for others. This preserves the intended keep-the-slot semantics and makes the documented contract true. Fix the stale doc comment either way.

REVISED FIX: The proposed continuation-based fix is correct in direction; refine it in three ways. (1) In the actor, keep the load Task as the single flight; waiters enroll a CheckedContinuation in an actor-held dictionary keyed by UUID, wrapped in withTaskCancellationHandler whose onCancel hops back to the actor to remove-and-resume that one continuation with CancellationError (remove-before-resume gives structural double-resume protection); a completion hook on the load Task resumes all remaining continuations with the result. (2) Distinguish the two CancellationError sources: waiter-cancel keeps the slot (intended PR #10 semantics), but if the load Task ITSELF finishes with any error — including CancellationError — clear the slot so retry works (today a CancellationError-shaped load failure would poison the slot forever). (3) Pin the now-real contract in SingleFlightLoaderTests: cancelled waiter throws CancellationError promptly (assert elapsed << load duration) AND the next caller still rejoins the in-flight load with count==1; update the file's doc comment and add a MurphySig Review line rather than editing the signed 2026-06-09 block. If a PR slot is tight, the minimum honest fix is doc-only: correct lines 46-52 and the branch comment to state that waiter cancellation does NOT interrupt the wait, and rely on the existing call-site Task.isCancelled guards — but the full fix is small (~60 lines + tests), Metal-free, and provable under swift test.

VERIFIER: CONFIRMED, by independent experiment and code trace. I wrote my own probe replicating SingleFlightLoader's exact shape (actor, unstructured Task slot, waiter awaiting task.value, waiter cancelled at 0.2s of a 3s load): output was "PROBE: cancelled waiter at 0.20s / PROBE: waiter got value 42 after 3.12s cancelled=true". Awaiting an unstructured Task's `.value` is not cancellation-responsive — the wait blocks to completion and never throws CancellationError for the waiter's own cancellation. So the doc contract at SingleFlightLoader.swift:46-52 ("if the calling task is cancelled, await task.val

---

## [18] startMic() leaves the tap installed when engine.start() throws — the next start() installs a second tap on the same bus and crashes
**Sources/M1K3Calls/StereoCallRecorder.swift:175** · dim=locks-and-queues · claimed=medium → verified=medium · effort=S

SCENARIO: startMic installs the input tap (line 168) then `try engine.start()` (line 175). If start() throws (input device wedged mid-route-flip, HAL error), the error propagates out of start() with `recording` still false and the tap left installed. On the user's retry, start() calls `_ = await stop()` — but stop()'s claim guard sees recording==false and returns nil WITHOUT removeTap — then startMic calls installTap on bus 0 again: AVAudioEngine raises the 'nullptr == Tap()' NSException and the app crashes, turning a transient audio-route error into a crash-on-retry during a live call.

FIX: In startMic, wrap the post-installTap steps so a throw removes the tap (`do { engine.prepare(); try engine.start() } catch { input.removeTap(onBus: 0); throw error }`), or make start()'s reset path unconditionally `engine.inputNode.removeTap(onBus: 0)` before reinstalling (removeTap on a bus with no tap is safe; installing over an existing one is not).

REVISED FIX: Use ONLY the first of the two proposed options: in startMic(), wrap the post-installTap steps — `do { engine.prepare(); try engine.start() } catch { input.removeTap(onBus: 0); throw error }` (optionally also clearing micConverter under the lock). Do NOT take the second option (unconditional removeTap in start()'s reset path before reinstalling): it is premised on "removeTap on a bus with no tap is safe", which directly contradicts the file's signed MurphySig review invariant at lines 92–93 ("removing a tap from a bus with none installed traps", claude-opus-4-8, 2026-06-09, PR #10). Even if that invariant is empirically wrong, shipping a fix that assumes its opposite requires an on-device re-proof first; the catch-and-removeTap fix is correct under either belief and stays consistent with the stop() claim-token design.

VERIFIER: Verified against the source. startMic() installs the input tap (StereoCallRecorder.swift:168) and the only subsequent throw point is `try engine.start()` (line 175) — all other guards throw before installTap. On that failure `recording` is still false (start() sets it at line 76 only after startMic succeeds), and stop()'s claim guard (lines 94–99) returns nil before removeTap (line 105, the file's only removeTap), so the tap is orphaned. The retry path is reachable in production: AppEnvironment.startRecording() (AppEnvironment.swift:1189–1203) catches the error, shows "Couldn't start recording

---

## [19] stopListening tears down the old streamer in a fire-and-forget Task — a quick restart races it and the late stop deafens the NEW session via the shared audioProcessor
**Sources/M1K3WhisperKit/WhisperKitProvider.swift:171** · dim=locks-and-queues · claimed=medium → verified=medium · effort=M

SCENARIO: stopListening does `Task { await streamer?.stopStreamTranscription() }` and returns. Every restart (startListening calls stopListening first — e.g. toggling voice mode off/on, or the error path's stop racing a re-engage) can interleave as: new AudioStreamTranscriber created from the SAME WhisperKit instance and startStreamTranscription() begins recording → the detached old-stop Task finally runs → AudioStreamTranscriber.stopStreamTranscription() calls audioProcessor.stopRecording() on the SHARED kit.audioProcessor → the new live session silently captures nothing. User symptom: mic on, WhisperKit tier selected, no transcript until they toggle listening again.

FIX: Serialise teardown-before-start: make stopListening async (await the streamer stop) with callers updated, or keep a per-provider serial coordination (e.g. store the stop Task and have startListening's launch Task `await` it before startStreamTranscription). A session generation check before yielding also hardens the callback path.

REVISED FIX: Take the auditor's SECOND option only, not the async-signature one. TranscriptionProvider.stopListening() is a sync protocol requirement shared with AppleSpeechTranscriber, and VoiceLoopController's dependencies are sync @MainActor closures — making stopListening async ripples through the protocol, both providers, the controller, MCPHostController, and the dictation path for no extra benefit. Single-file fix in WhisperKitProvider: store the teardown as `private var pendingStop: Task<Void, Never>?` (lock-guarded like the other fields); stopListening assigns it instead of a bare Task; startListening's launch Task does `await pendingStop?.value` before `streamer.startStreamTranscription()`. Add a session-generation counter checked in the onState callback and in the error-path `self.stopListening()` (line 161) so a stale streamer's late callback can't write the new session's lastText and a stale start-Task failure can't tear down a newer session — that error path has the same class of bug.

VERIFIER: Every link verified against real sources. (1) Shared resource: startListening builds each new AudioStreamTranscriber from the SAME cached kit.audioProcessor (WhisperKitProvider.swift:141); WhisperKit's stopStreamTranscription() calls audioProcessor.stopRecording() (checkouts/WhisperKit/.../AudioStreamTranscriber.swift:89-93), which removes the tap, disconnects the input node, and stops the engine (AudioProcessor.swift stopRecording) — a late old-stop kills the NEW session's recording; the new streamer's realtimeLoop then spins on 'Waiting for speech...' with a dead mic. (2) Two reachable unord

---

## [20] NSLock + @unchecked Sendable where its own siblings already use Mutex<T> + checked Sendable
**Sources/M1K3Knowledge/SwappableEmbeddingService.swift:27** · dim=locks-and-queues · claimed=low → verified=low · effort=S

SCENARIO: No live race — this is the modernization half of the lock audit. SwappableInferenceProvider and SwappableSpeechProvider (which this file's header says it mirrors) already use `Mutex<any P>` from Synchronization and are plain `Sendable`; this façade kept the pre-relocation NSLock + `@unchecked Sendable`, so the compiler verifies nothing here and any future added mutable property silently rides outside the lock. Same pattern in RuntimeSelectionBox (M1K3App/RuntimeInferenceProvider.swift:22) and ToolCallRecorder (M1K3App/ChatEvalStage.swift:55).

FIX: Replace with `private let current: Mutex<any EmbeddingService>` and drop `@unchecked` — byte-for-byte the sibling shape, compiler-checked Sendable, EmbedQuerySeamTests already pin behaviour. Ride RuntimeSelectionBox and ToolCallRecorder along in the same PR (identical trivial boxes).

REVISED FIX: Proposed fix is correct as stated. Two additions: (1) keep the explicit embedQuery forward and its pinning comment — EmbedQuerySeamTests guards against the protocol-default trap; (2) add `import Synchronization` and a MurphySig Review line noting the convergence on the siblings' Mutex shape (the header's "mirrors SwappableInferenceProvider/SwappableSpeechProvider" makes this a fidelity restore, not a divergence).

VERIFIER: Verified all three locations. SwappableEmbeddingService (Sources/M1K3Knowledge/SwappableEmbeddingService.swift:26-28) uses NSLock + @unchecked Sendable while its named siblings SwappableInferenceProvider (Sources/M1K3Inference/SwappableInferenceProvider.swift:26, Mutex<any InferenceProvider> + checked Sendable) and SwappableSpeechProvider (Sources/M1K3Voice/SwappableSpeechProvider.swift:33, Mutex<State>) already use the Synchronization.Mutex house pattern. RuntimeSelectionBox (M1K3App/RuntimeInferenceProvider.swift:22) and ToolCallRecorder (M1K3App/ChatEvalStage.swift:55) match the claim too. 

---

## [21] Touch ID-gated Keychain read blocks the main actor for the whole biometric prompt at every launch
**M1K3App/AppEnvironment+CallStore.swift:42** · dim=mainactor-and-hangs · claimed=high → verified=medium · effort=M

SCENARIO: AppDelegate.applicationDidFinishLaunching runs `try AppEnvironment()` inside Task { @MainActor } (M1K3App.swift:226). Init synchronously calls makeCallPersistence (AppEnvironment.swift:596), which calls `provider.symmetricKey()` — a SecItemCopyMatching on a .userPresence-protected item (KeyStore.swift:63) that surfaces the Touch ID sheet and BLOCKS the calling thread until the user authenticates or cancels. That thread is the main thread: while the sheet is up, the 'M1K3…' ProgressView spinner freezes, the menu-bar item is dead, and the rest of composition (MCP server start, brain warm, chat restore) is stalled. A user who ignores or walks away from the prompt leaves the app beachballed indefinitely — an unbounded main-thread hang on EVERY launch, since the key is read once per launch by design.

FIX: Don't pay the biometric read on the launch critical path. Make callPersistence a lazily-resolved seam: construct AppEnvironment with a placeholder/deferred store and resolve the real GRDBCallPersistence (key read included) off the main actor on first Calls use — e.g. an async factory awaited via Task.detached the first time startRecording/calls()/importCallTranscript needs it, with the recoverParkedRecordings launch chain awaiting the same single-flight resolver. This also moves the prompt to the moment the user actually touches Calls, which is a better consent UX than an unexplained launch prompt. Minimum viable fix if the per-launch prompt is wanted: wrap makeCallPersistence in `await Task.detached { ... }.value` inside an async init/factory so the main actor stays free while SecItemCopyMatching blocks.

REVISED FIX: Keep the launch-time prompt (signed decision) but stop blocking the main actor on it: make AppEnvironment.init async (its one production call site, M1K3App.swift:226, is already inside Task { @MainActor }; SelfTest composes separately) and resolve the store via `callPersistence = await Task.detached { Self.makeCallPersistence(at: callsURL) }.value` — legal since `any CallPersistence` is Sendable. This keeps the spinner animating, the menu bar and Quit responsive, and Cancel/auth semantics identical. Note it does NOT unstall the rest of composition (MCP host start at init:610 still waits for the prompt — that ordering is part of the signed design's cost); if Kev wants the prompt moved to first Calls use, that is a separate product decision to put to him, not to ship in this fix, because it changes the signed one-prompt-per-launch behavior and the parked-recordings launch recovery would re-trigger the prompt at launch anyway.

VERIFIER: The blocking chain is fully verified: AppEnvironment is @MainActor (AppEnvironment.swift:89), constructed at M1K3App.swift:226 in Task { @MainActor }; init line 596 synchronously reaches SecItemCopyMatching on a .userPresence item (KeyStore.swift:63), which presents the system auth sheet and blocks the calling thread until the user responds. The key is created (without prompt) on first launch, so every subsequent launch pays a prompt with the main thread blocked — corroborated as live behavior by the 2026-06-12 session record (shipped + on-device QA'd) and in-code observed-behavior comments (e

---

## [22] Drag-and-drop ingest reads the whole file (Data/String contentsOf) synchronously on the main actor
**M1K3App/AppEnvironment.swift:656** · dim=mainactor-and-hangs · claimed=medium → verified=medium · effort=S

SCENARIO: `ingest(url:)` is @MainActor and executes `let data = try Data(contentsOf: url)` (PDF) or `String(contentsOf:)` (text) before its first await. Dropping a 300 MB scanned PDF onto the window blocks the main thread for the full disk read + allocation — multi-second beachball with no progress UI, and the isIngesting spinner state it just set can't even render because the render loop is blocked. Same defect duplicated in the iOS shell (M1K3iOSApp/AppCore.swift:374-377), where a big Files-app import freezes the UI on device.

FIX: Move the byte read off the main actor: `let data = try await Task.detached { try Data(contentsOf: url) }.value` (security-scoped access is process-wide, so start/stop scoping around the detached read still works — keep startAccessing on the caller and stop after the await), or add `ingest(fileURL:)` to DocumentIngester (nonisolated async, so the read + extraction run on the global executor) and have AppEnvironment/AppCore pass the URL through. Fix both shells in the same PR.

REVISED FIX: Prefer the auditor's second option, refined: add URL-based entry points to DocumentIngester (nonisolated async) — e.g. ingestPDF(title:url:sourceRef:) using the ALREADY-EXISTING PDFTextExtractor.extract(url:) (PDFTextExtractor.swift:31), and ingest(title:fileURL:...) that reads String(contentsOf:) inside the nonisolated method — so the byte read and extraction both run on the global executor. Both shells (AppEnvironment.ingest(url:) and AppCore.ingest(url:)) then just pass the URL through, keeping startAccessingSecurityScopedResource/stop on the caller spanning the await (scope is process-wide, so this is sound). This avoids Task.detached in the app shells, reuses the existing URL extractor, gets unit-test coverage in the package (the app shells are untested), and fixes macOS + iOS in one PR.

VERIFIER: Verified against source: AppEnvironment is @MainActor (AppEnvironment.swift:89-91) and ingest(url:) performs Data(contentsOf:) at :656 / String(contentsOf:) at :659 synchronously before its first await, immediately after setting isIngesting=true — the main thread blocks for the full read so the spinner cannot render. Callers (ContentView.swift:169, :216) are MainActor-inherited Tasks; nothing hops off-main. The iOS duplicate at M1K3iOSApp/AppCore.swift:374/:377 is confirmed identical (AppCore is @MainActor, :42-44) and is worse there because Files-app/iCloud URLs can stall the synchronous read

---

## [23] DocumentsView does an N+1 full-content chunk fetch on the main thread per row per render just to show a count
**M1K3App/DocumentsView.swift:49** · dim=mainactor-and-hangs · claimed=medium → verified=low · effort=S

SCENARIO: Each DocumentRow evaluates `env.chunkCount(for: doc.id)` in the List body. chunkCount (AppEnvironment.swift:1061-1063) calls `store.chunks(forItem:)` which is `SELECT * FROM knowledge_chunks` — it fetches and decodes EVERY chunk's full `content` text (KnowledgeStore.swift:351-373) only to `.count` it, synchronously on the main thread, inside SwiftUI body evaluation. With the live ~1200-chunk corpus, opening the Documents sheet decodes the entire corpus text on main; every body re-evaluation (toggling show-internal, a delete, observation-driven re-render) repeats it. On a document-heavy store this is a visible stall each time the sheet opens or re-renders.

FIX: Add `KnowledgeStore.chunkCount(forItem: UUID) -> Int` doing `SELECT COUNT(*) FROM knowledge_chunks WHERE item_id = ?` (the global `chunkCount()` sibling at KnowledgeStore.swift:258 is the template), route AppEnvironment.chunkCount through it, and compute the counts once in `reload()` into a `[UUID: Int]` alongside `docs` instead of per-row in body — one query pass per reload, zero DB work in body.

REVISED FIX: As proposed, with one improvement: instead of N per-item COUNT queries in reload(), add `KnowledgeStore.chunkCounts() -> [UUID: Int]` doing a single `SELECT item_id, COUNT(*) FROM knowledge_chunks GROUP BY item_id` (red-first test alongside KnowledgeStoreFetchTests), have DocumentsView.reload() fetch it once into a `@State private var chunkCounts: [UUID: Int]`, and pass `chunkCounts[doc.id] ?? 0` to DocumentRow. Zero DB work in body, one query per reload. Keep or retire AppEnvironment.chunkCount(for:) accordingly (it has no other callers).

VERIFIER: The mechanics of the claim all verify against the code. DocumentsView.swift:49 and :59 both evaluate `env.chunkCount(for: doc.id)` inside the List's ForEach row closures. AppEnvironment is `@MainActor` (AppEnvironment.swift:89-91) and `chunkCount(for:)` (AppEnvironment.swift:1061-1063) is `(try? store.chunks(forItem: id).count) ?? 0`. KnowledgeStore.chunks(forItem:) (KnowledgeStore.swift:351-371) runs `SELECT * FROM knowledge_chunks WHERE item_id = ? ORDER BY ordinal` through a synchronous `dbQueue.read` and materialises a full KnowledgeChunk — including `content: row["content"] ?? ""` — for e

---

## [24] Calls surface decrypts and decodes every stored call synchronously on the main thread; save/import writes encrypt on main too
**M1K3App/CallsView.swift:38** · dim=mainactor-and-hangs · claimed=medium → verified=medium · effort=M

SCENARIO: CallsView `.task { calls = env.calls() }` and `.onChange(of: env.callCount)` both call `env.calls()` — a synchronous `callPersistence.loadAll()` that AES-GCM-decrypts and JSON-decodes EVERY call session (full transcripts + summaries) on the main thread before the first await (GRDBCallPersistence.swift:64-70). A user with months of recorded calls pays a full-store decrypt on every Calls-tab open AND after every save (callCount change re-triggers it). The write side mirrors it: `importCallTranscript` does `String(contentsOf:)` at AppEnvironment.swift:1120 and `try callPersistence.save(session)` at :1137, and `processRecording` saves at :1721 — whole-transcript encrypt + sqlite write on the main actor, stalling the UI right as the 'Transcribed…' status should appear.

FIX: Give the view an async load: `func calls() async -> [CallSession]` that wraps loadAll in `Task.detached` (callPersistence is a Sendable GRDB handle — same pattern ConstellationWindow.rebuildIfChanged already uses), and consider a summary-only `listAll()` (id/title/date, no segment decode) for the list with lazy per-call load on selection. Move `save` calls behind the same detached hop, and the `String(contentsOf:)` per finding 3's pattern.

REVISED FIX: Core fix only: make the read path async — `func calls() async -> [CallSession]` hopping loadAll to a detached utility task (the ConstellationWindow pattern; persistence is Sendable), with CallsView awaiting it in .task/.onChange plus a load-generation guard so a stale onChange load can't overwrite a newer one. Drop the summary-only listAll() from this PR — the signed GRDBCallPersistence design keeps title/summary inside the encrypted payload, so a cheap listing needs a schema/privacy decision (Kev's call), not a quick win. Write-side saves may ride the same seam opportunistically but are not the payoff; do not claim the 'Transcribed…' stall as motivation.

VERIFIER: Every load-path claim verified: AppEnvironment is @MainActor (line 89-91); calls() (:1148-1150) synchronously runs GRDBCallPersistence.loadAll() (:64-72) which AES-GCM-decrypts and JSON-decodes every stored session — full transcripts included, since the signed privacy design puts everything except started_at inside the encrypted payload blob. CallsView.swift:38-39 triggers this on sheet open and on every callCount change. The proposed off-main hop is type-safe (CallPersistence: Sendable, GRDBCallPersistence @unchecked Sendable) and matches the in-repo ConstellationWindow.rebuildIfChanged prece

---

## [25] Chat persistence re-encodes the ENTIRE conversation JSON on the main actor after every turn; conversation switch decodes it there too
**Sources/M1K3Chat/ChatSession.swift:380** · dim=mainactor-and-hangs · claimed=low → verified=low · effort=M

SCENARIO: ChatSession is @MainActor. persistActiveConversation (called at the end of every send, plus on switch/new/exit) runs `JSONEncoder().encode(messages)` over the whole transcript — every message with sources, reasoning, citations, followUps — then a synchronous DatabaseQueue write (ChatHistoryStore.swift:118-131). Cost grows linearly with conversation length: a months-old daily conversation is a multi-MB encode on the main thread, landing as a hitch exactly when the answer completes (competing with the final message re-render and avatar transition). `switchTo` (ChatSession+Conversations.swift:41) pays the mirror-image full decode on main when the user picks a conversation in the history drawer.

FIX: Move payload codec + write off the main actor: snapshot `messages` (ChatMessage is a Sendable value type) and hand it to a serial background writer — e.g. `let snapshot = messages; Task.detached { try history.save(id:, messages: snapshot, updatedAt:) }` with a small single-flight/coalesce latch so rapid saves collapse, keeping historyRevision bump on main. For switchTo, load + decode in a detached task and apply via setActiveConversation on return. Longer term, an append-per-message schema kills the O(conversation) rewrite entirely.

REVISED FIX: Keep the blob-per-conversation schema (signed decision — add a MurphySig Review line noting the 'conversations are small' premise is being shored up, not overturned). Step 1 (house measure-first doctrine): add a .notice-level duration breadcrumb around history.save (.info/.debug don't persist in OSLogStore) to size the real cost before restructuring. Step 2: move the per-turn save off main via a SERIAL persist pipeline owned by ChatSession — snapshot messages + activeConversationID (ChatMessage is Sendable), enqueue latest-wins-coalesced onto a single background writer, and expose a `persistTask` test hook exactly like titlingTask so ChatSessionConversationsTests stay deterministic (await the hook, don't sleep). Critically, route deleteConversation through the SAME writer (or version-check before write) so an in-flight save cannot resurrect a deleted row via the upsert's INSERT path; bump historyRevision after the write completes. Leave switchTo synchronous for now — it is a one-off user-initiated tap, and making it async requires a switch-generation token plus an interim UI state for marginal gain; fix it only if the Step-1 breadcrumb shows decode times that justify the extra state machine.

VERIFIER: Verified in source: ChatSession is @MainActor; persistActiveConversation (ChatSession.swift:377) synchronously runs JSONEncoder().encode over the whole transcript + a blocking dbQueue.write of the full blob (ChatHistoryStore.swift:118-135) at the end of every send (:368); switchTo (ChatSession+Conversations.swift:41) pays the full decode on main. Payload grows O(conversation) — persisted fields include full ChunkHit.content source text and reasoning chains; nothing rotates conversations. So the defect class is real and compounds per turn, and the hitch lands exactly at answer completion where 

---

## [26] Call recording accumulates unbounded PCM in RAM, with megabyte-scale array reallocs under an NSLock shared with the audio capture threads
**Sources/M1K3Calls/StereoCallRecorder.swift:172** · dim=polling-and-priority · claimed=high → verified=high · effort=M

SCENARIO: A 60-minute call at 48kHz accumulates ~172.8M floats per channel (~690MB near + ~690MB far, ~1.4GB total), held entirely in memory until stop() writes the file. Two concrete failures: (1) memory pressure / jetsam kills the app mid-call and the ENTIRE recording is lost — nothing is on disk until stop(); (2) every capacity doubling of nearSamples copies hundreds of MB while holding `lock` INSIDE the mic tap callback (an AVAudioEngine I/O-thread callback); the SCStream captureQueue callback and stop() block on that same NSLock (no priority donation to the holder), so long-call reallocs stall high-priority capture delivery — dropped input buffers, i.e. gaps in the very recording being made.

FIX: Stream to disk incrementally: have the tap/SCStream callbacks hand fixed-size chunks to a dedicated writer (extend CallAudioWriter with an append-to-.partial mode, finalize/validate/rename at stop — keeps the atomicity story). At minimum, drain samples periodically to bounded ring buffers, and `reserveCapacity` so reallocs never happen inside the capture callbacks. Crash-interrupted .partial files can then feed the existing ParkedRecordings recovery.

REVISED FIX: Stream to disk incrementally, keeping the atomicity story: extend CallAudioWriter with an append mode (AVAudioFile supports incremental writes) fed by a dedicated writer draining bounded per-channel buffers — callbacks only append to small chunks under the lock, never megabyte reallocs (reserve chunk capacity; a fixed reserveCapacity for the whole call cannot work for unbounded duration). Because near/far arrive at independent rates, either write two mono .partial files and mux+validate+rename at stop(), or pad-align at finalize. For crash recovery: ParkedRecordings.pending() test-pins '.partial never surfaces' and CallAudioWriter removes partials on failure — use a distinct suffix (e.g. .recovering) or amend that pinned contract so crash-interrupted partials are validated (CAF is append-friendly, playable to last flush) before being offered to the existing recovery flow. This also eliminates the 2x peak at stop() (the full-length AVAudioPCMBuffer in CallAudioWriter.swift:82-92) and the no-crash total-loss path when that allocation fails.

VERIFIER: Verified in source: both capture callbacks (StereoCallRecorder.swift:172 mic tap, :288 SCStream) append to unbounded [Float] arrays under a shared NSLock, and the only disk write is CallAudioWriter.write at stop() (:121). No duration cap exists anywhere in the call path (AppEnvironment.startRecording/stopRecording checked). The ~1.4GB/hour math is correct. Verification found the situation WORSE than claimed: CallAudioWriter.swift:82-92 allocates a second full-length stereo AVAudioPCMBuffer at stop(), so peak memory is ~2x the recording (~2.8GB/hour-call), and if that allocation or write fails 

---

## [27] Word clock polls player.isPlaying/lastRenderTime/playerTime at 30 Hz on the MainActor — the exact cross-QoS engine-lock inversion SpeakingState was built to eliminate (the likely '#4 audio priority-inversion')
**Sources/M1K3Voice/EffectfulSpeechProvider+Streaming.swift:251** · dim=polling-and-priority · claimed=medium → verified=medium · effort=M

SCENARIO: startClockIfNeeded's task inherits @MainActor; every 33ms tick() calls playerSampleNow(), which reads `player.isPlaying`, `player.lastRenderTime` and `player.playerTime(forNodeTime:)`. The codebase's own doctrine (EffectfulSpeechProvider.swift:181-186) states these take an internal AVAudioEngine lock the render thread holds, and that polling them from a high-QoS caller ~30fps 'inverts priority (the TPC hang risk)' — which is why isSpeaking() was converted to the cached SpeakingState flag. The word clock re-introduces the identical pattern for the full duration of every spoken answer: while the render thread (or the deferred utility-priority node.stop() from finding above) holds the engine lock, the main thread blocks — UI hitches during speech, Thread-Performance-Checker flags, and in the worst case the main thread waits behind a lower-QoS lock holder with no donation. This polling+inversion combination is the concrete candidate for the long-open '#4 audio priority-inversion (TPC pass)' bench item.

FIX: Stop touching the engine from the tick: schedule() already reads playerSampleNow() once per chunk to anchor the accumulator — record (anchorSample, ContinuousClock.now) there and have tick() extrapolate the current sample as anchor + elapsed×sampleRate, using the session's own state (buffers scheduled, not cancelled) instead of `player.isPlaying`. Engine reads then happen ~once per sentence at a natural boundary instead of 30×/s. Verify with a TPC-enabled on-device run (closes bench item #4 either way).

REVISED FIX: The anchor-and-extrapolate direction is right but needs one refinement: for the FIRST chunk, schedule() runs before player.play(), so playerSampleNow() returns nil (the isPlaying guard fails) and there is no anchor sample to record there. Anchor at play()-start instead — record (sample: 0, ContinuousClock.now) the first time player.play() actually starts playback (or on the first successful post-play engine read, done once, off the 30 Hz path) — and re-anchor at each subsequent chunk join where schedule()'s existing single playerSampleNow() read already happens (preserving the probe-verified dry-gap re-anchor semantics in UtteranceTimelineAccumulator). tick() then extrapolates currentSample = anchor + elapsed × sampleRate and gates on session state (buffers pending, not cancelled) instead of player.isPlaying; route-change/stop paths already cancel the session so a stopped engine can't keep firing words. Drop the claim's node.stop() sub-scenario from the rationale (it's MainActor-serialized). Verify with a TPC-enabled on-device run; the extrapolation maths is pure and unit-testable alongside UtteranceTimelineAccumulator.

VERIFIER: Verified in source: tick() inherits MainActor (StreamingPlaybackSession is @MainActor, the clock Task is non-detached) and polls player.isPlaying/lastRenderTime/playerTime(forNodeTime:) at ~30 Hz for the whole of every spoken answer (EffectfulSpeechProvider+Streaming.swift:232-257). The codebase's own signed doctrine (SpeakingState.swift:9-13, Confidence 0.9, 2026-07-01) states these exact reads take an internal AVAudioEngine lock held by a Default-QoS thread and that polling them ~30fps from a high-QoS caller was a Thread-Performance-Checker-flagged priority-inversion hang risk — the very rea

---

## [28] Multi-file drop/import spawns unstructured concurrent ingest tasks, corrupting isIngesting and the status banner
**M1K3App/ContentView.swift:168** · dim=await-parallelism · claimed=medium → verified=medium · effort=S

SCENARIO: Drop 3 PDFs onto the chat window (or multi-select in the fileImporter at line 215): three unstructured Tasks all enter env.ingest concurrently on MainActor. Each sets isIngesting=true; the FIRST to finish runs its `defer { isIngesting = false }` while the other two are still extracting/embedding, so the GreetingCard 'Reading it now…' state and IngestBanner busy spinner (ContentView.swift:251, GreetingCard.swift:97) go idle mid-work. lastIngestStatus is then overwritten by each finisher, so the user sees only the last file's result and any failure banner for an earlier file (which is designed to persist, line 670-674) is silently clobbered. There is no re-entrancy guard on ingest(url:) unlike processRecording's isTranscribingCall.

FIX: Replace the per-URL fire-and-forget Tasks with ONE Task iterating the URLs sequentially (matching the deliberate serialization pattern in recoverParkedRecordings) — `Task { for url in urls { await env.ingest(url: url) } }` — at both the dropDestination (line 167) and fileImporter (line 214) sites. Optionally make isIngesting a counter if true concurrency is ever wanted. Sequential is honest here anyway: the embeds serialize on the MLX container actor regardless.

REVISED FIX: The proposed fix is correct as-is: replace per-URL Tasks with one Task iterating sequentially at both ContentView.swift:167 and :214 — `Task { for url in urls { await env.ingest(url: url) } }` — matching the documented serial-by-design precedent at AppEnvironment.swift:1651. Optionally add a one-line comment referencing that precedent. Do not attempt to also fix the single-slot lastIngestStatus (only-last-file-result) behavior in the same change — that is pre-existing design, not this defect.

VERIFIER: CONFIRMED, tried to refute and could not. Verified against source: (1) Both call sites exist exactly as claimed — ContentView.swift:167-172 (dropDestination) and 209-218 (fileImporter with allowsMultipleSelection: true) spawn one unstructured `Task { await env.ingest(url: url) }` per URL. (2) AppEnvironment is @MainActor @Observable (AppEnvironment.swift:89-91), so there is no Swift data race — but MainActor isolation does NOT prevent this: `ingest(url:)` (line 645) suspends at `await ingester.ingestPDF/ingest` and `await spotlightDonate`, so the three tasks interleave at those suspension poin

---

## [29] No embedBatch override — bulk ingest/reindex pays N sequential single-text forwards with per-embed cache reclaim
**Sources/M1K3MLX/MLXEmbeddingService.swift:102** · dim=await-parallelism · claimed=medium → verified=low · effort=M

SCENARIO: MLXEmbeddingService only implements embed(_:), so every bulk path — KnowledgeStore.reindexEmbeddings (line 217, embedBatch over the WHOLE corpus: measured ~2 min for 1201 chunks on 07-08), MemoryStore.reindexEmbeddings:704, DocumentIngester.ingest:71 (a large dropped PDF = hundreds of chunks), CallIngester.ingest:58 (a long call) — falls through to the protocol-extension loop in EmbeddingService.swift:67 that awaits embed() per text: one container.perform actor round-trip, one un-batched GPU forward, and one MLXMemoryBudget.reclaim per chunk. A 300-chunk PDF drop keeps the 'Indexed…' banner busy for tens of seconds; every embedder swap/fingerprint bump re-pays the full 2-minute corpus tax; the ingest-status thermal footprint is maximal.

FIX: Override embedBatch in MLXEmbeddingService: inside ONE container.perform, tokenize a mini-batch (e.g. 8–16 texts), pad to the max length, build the attention mask (context.pooling already takes a mask), run one batched forward, split + MRL-truncate the rows; reclaim once per mini-batch instead of per text. The protocol comment already invites this ('backends with a native batch path should override'). Keep the per-batch reclaim to preserve the peak-memory doctrine; add a cancellation check between batches so a thermal defer can abort a reindex.

REVISED FIX: Override embedBatch in MLXEmbeddingService with internal mini-batching (8–16 texts), but two corrections to the proposal: (1) RIGHT-padding is load-bearing, and the attentionMask passed to the model is inert — Qwen3Model.callAsFunction IGNORES its attentionMask parameter entirely (mlx-swift-lm Models/Qwen3.swift: `model(inputIds, cache: nil)`; the inner model builds its own purely causal mask). Right padding keeps real-token hidden states and RoPE positions exactly equal to the unbatched run (causal attention never looks at trailing padding); left padding would silently corrupt every vector. The mask matters only for context.pooling's last-token selection (Pooling.last does takeAlong at per-row tokenCount-1 — mask-aware, right-pad-correct). (2) Mini-batching is mandatory, not a tuning knob: KnowledgeStore.reindexEmbeddings:217 passes the entire corpus (1201+ texts) in ONE embedBatch call, so a single padded forward would be a memory bomb. Keep reclaim per mini-batch (preserves the in-file per-embed-reclaim doctrine's intent — bounded peak — while amortizing 1201 reclaims to ~80), add Task.checkCancellation between mini-batches, and pin correctness with an MLX-integration cosine-parity test (batched vs single-embed vectors cosine ≈ 1.0 across heterogeneous lengths, i.e. rows that actually get padded).

VERIFIER: All factual claims verified against source: MLXEmbeddingService.swift has no embedBatch override (only embed/embedQuery/isAvailable), so it inherits the per-text loop at EmbeddingService.swift:67; the protocol comment at line 41 explicitly invites a native batch override. All four bulk callers confirmed (KnowledgeStore.reindexEmbeddings:217 passes the WHOLE corpus in one embedBatch call; MemoryStore.reindexEmbeddings:704; DocumentIngester.ingest:71; CallIngester.ingest:58), and SwappableEmbeddingService:54 forwards to the active backend, so all reach the inherited loop. Each iteration pays an 

---

## [30] Full-conversation JSON encode + synchronous SQLite write on MainActor after every turn
**Sources/M1K3Chat/ChatSession.swift:380** · dim=await-parallelism · claimed=medium → verified=medium · effort=M

SCENARIO: persistActiveConversation (called at the end of EVERY send, plus each conversation switch/exit) calls GRDBChatHistoryStore.save, which JSON-encodes the ENTIRE messages array — including reasoning traces, sources, and full HTML/code artifact bodies stored in message text — and performs a blocking dbQueue.write + fsync, all on the MainActor (ChatSession is @MainActor and save is a synchronous call). Cost grows O(total conversation size) per turn: a long session carrying a few house-sheet artifacts re-serializes hundreds of KB and blocks the main thread for tens of ms right at the moment the final answer text/citations are being rendered — visible hitch at the end of each turn. ChatSession.init (lines 247-254) does the mirror-image blocking list()+loadMessages JSON decode on the launch path.

FIX: Make ChatHistoryPersisting.save async and route it through GRDB's async write (or capture the messages value and hop off-main with a Task before encode+write, serialized by conversation id so writes can't reorder). The encode must move off-main too — it's the O(n) half. loadMessages on the launch restore path can stay sync (one-time) or move behind an async restore.

REVISED FIX: The finding is real but the proposed fix's fire-and-forget option is wrong for this codebase. Do NOT detach the save into an unawaited Task: (a) ~10 pinned tests assert store contents immediately after `await session.send` (e.g. ChatSessionConversationsTests.swift:204-205 sendPersists, 214-217 failedSendPersists) and would flake; (b) deleteConversation is guarded only by isResponding, so a pending detached upsert landing after the DELETE resurrects the deleted row; (c) switchTo's documented invariant ("the current one is already persisted") would silently break. Correct fix: make ChatHistoryPersisting.save async; implement GRDBChatHistoryStore.save via GRDB's async `dbQueue.write { }` with the JSONEncoder().encode moved INSIDE the off-main closure (capture the [ChatMessage] value — it's Sendable); AWAIT the save inside send before it returns (the isResponding defer hasn't fired yet at line 368, so the existing switch/delete/new-conversation guards keep excluding overlap, ordering is preserved, and all pinned tests pass unchanged). Keep the error-log-and-continue behaviour and the historyRevision bump after the await. Leave the init-time restore synchronous (one-time launch cost, and making init async ripples into AppEnvironment) — or name it a separate follow-up. Note the mirror-image blocking loadMessages on switchTo as a lesser sibling that can ride the same protocol change or stay sync.

VERIFIER: Verified in source: ChatSession is @MainActor (ChatSession.swift:178); persistActiveConversation (line 377) is synchronous, called at the end of every send (line 368, its only caller); GRDBChatHistoryStore.save (ChatHistoryStore.swift:118-135) JSON-encodes the ENTIRE messages array then does a blocking dbQueue.write, all on the main thread. The payload genuinely grows O(conversation): CodingKeys persist `reasoning` and `sources`, each ChunkHit carries full chunk `content` (KnowledgeModels.swift:118), and artifact HTML lives in message text. The init-path blocking list()+loadMessages (lines 247

---

## [31] Synchronous Data(contentsOf:)/String(contentsOf:) file reads on MainActor in both shells' ingest
**M1K3App/AppEnvironment.swift:656** · dim=await-parallelism · claimed=medium → verified=medium · effort=S

SCENARIO: AppEnvironment.ingest(url:) is @MainActor; lines 656/659 read the dropped file's full bytes synchronously on the main thread before handing off to the (off-actor) DocumentIngester. Drop a 150 MB scanned PDF: the UI beachballs for the entire disk read BEFORE the 'Reading it now…' state can even render a frame. The iOS shell has the identical pattern (M1K3iOSApp/AppCore.swift:374-377), where large Files-app picks over slow storage make it worse.

FIX: Move the byte read off-main: wrap the read+extract in the already-async ingester call (e.g. give DocumentIngester an ingestPDF(fileURL:) that reads inside its nonisolated async body), or `let data = try await Task.detached { try Data(contentsOf: url) }.value` while keeping the security-scoped access alive around it. Apply the same fix to AppCore.ingest on iOS.

REVISED FIX: Prefer the auditor's first option over the Task.detached one: add DocumentIngester.ingestPDF(title:fileURL:sourceRef:) and ingest(title:fileURL:...) overloads that read the bytes inside their nonisolated async bodies (off-main under current package settings — no NonisolatedNonsendingByDefault), and switch both shells (AppEnvironment.ingest at macos/M1K3App/AppEnvironment.swift:656-660, AppCore.ingest at macos/M1K3iOSApp/AppCore.swift:374-378) to pass the URL. Keep startAccessingSecurityScopedResource/defer in the callers — scoped access is process-wide, so it stays valid across the await. If the package ever adopts NonisolatedNonsendingByDefault/approachable concurrency, revisit: the ingester bodies would then inherit the caller's actor and the read would land back on main — annotate the overloads @concurrent (or keep an explicit off-actor read) to pin the guarantee.

VERIFIER: Confirmed line-by-line. AppEnvironment is @MainActor @Observable (AppEnvironment.swift:89-91); ingest(url:) sets isIngesting=true (646) then synchronously reads Data(contentsOf:) (656) or String(contentsOf:) (659) on the main thread with no suspension point in between, so the GreetingCard's "Reading it now…" busy state (GreetingCard.swift:16,97 — the exact string the auditor cited) cannot render a frame until the read completes. The iOS twin is verbatim (AppCore.swift:42-44 @MainActor; 374/377). Refutation attempts failed: DocumentIngester is a Sendable struct with nonisolated async methods an

---

## [32] FileDownloader stages HTTP error bodies as Kokoro model weights — no status-code validation (and misses cancellation)
**Sources/M1K3Kokoro/KokoroSpeechProvider.swift:271** · dim=modern-api-adoption · claimed=medium → verified=medium · effort=S

SCENARIO: GitHub serves a 429/503 (release-asset rate limiting is real) during the first M1K3-Voice prepare. URLSession does NOT treat HTTP error statuses as errors: urlSession(_:downloadTask:didFinishDownloadingTo:) fires with the error-page body, moves it to kokoro-v1.0.onnx, and resumes the continuation as success. isModelStaged is then true forever, so every later launch restores the 'M1K3 Voice' tier, synthesizer.preload() fails, and the user permanently gets the Apple fallback voice with only a log line — no retry path exists because prepare() returns instantly once both files 'exist'. Separately, the hand-rolled CheckedContinuation never observes Task cancellation, so a cancelled prepare hangs until the ~326 MB download finishes.

FIX: Minimal: keep the delegate but take `downloadTask` (not `_`) in didFinishDownloadingTo and fail unless `(downloadTask.response as? HTTPURLResponse).map { 200..<300 ~= $0.statusCode } == true`. Better (the modernization): replace the whole 65-line delegate class — its @unchecked Sendable, write-once-continuation proof, and exactly-once dance — with `URLSession.shared.bytes(from:)`: validate `response` status up front, stream bytes to a `.partial` file, report progress from `response.expectedContentLength`, rename on completion. That shape is cancellation-correct for free (the byte loop throws on cancel) and mirrors CallAudioWriter's proven partial-then-rename pattern.

REVISED FIX: The proposed minimal fix is correct and compatible — take `downloadTask` in didFinishDownloadingTo and `continuation?.resume(throwing:)` unless `(downloadTask.response as? HTTPURLResponse).map({ 200..<300 ~= $0.statusCode }) == true`, BEFORE moving the file (so nothing gets staged on error). Two refinements: (a) ship the minimal fix, not the bytes(from:) rewrite, as the PR — the 65-line delegate replacement is a fine follow-up modernization but is a bigger diff in a heavy target for zero additional user-visible payoff today, and the cancellation gap it closes is unreachable (no caller ever cancels prepare; prepareM1K3Voice runs in a fire-and-forget Task guarded by isPreparingVoice). (b) Consider one extra line of self-heal: since external TestFlight users exist, `isModelStaged` (or the prepare fast path) could reject a kokoro-v1.0.onnx under a sanity floor (e.g. 1 MB vs the real ~326 MB) and delete it, so any already-poisoned install re-downloads instead of being stuck forever — the status check alone only protects future downloads.

VERIFIER: VERIFIED REAL — every link in the claimed chain checks out in source. (1) FileDownloader (KokoroSpeechProvider.swift:232-296) has zero status-code validation: didFinishDownloadingTo ignores its downloadTask parameter entirely (`downloadTask _:`), moves whatever body landed in the temp file to the destination, and resumes the continuation as success; didCompleteWithError is nil for HTTP 4xx/5xx by documented URLSession semantics, so an error page IS staged as kokoro-v1.0.onnx. (2) The permanent-poisoning consequence is confirmed in AppEnvironment.swift: prepareM1K3Voice (line 1852) sees prepare

---

## [33] Both @preconcurrency AVFoundation imports in M1K3Calls are dead — proven by build — and are suppressing future Sendable diagnostics in the one file that smuggles buffers across a Sendable boundary
**Sources/M1K3Calls/StereoCallRecorder.swift:32** · dim=modern-api-adoption · claimed=low → verified=low · effort=S

SCENARIO: Verified empirically this session: removing `@preconcurrency` from StereoCallRecorder.swift:32 and CallAudioWriter.swift:21 builds M1K3Calls clean (zero diagnostics, Swift 6 strict mode). While present, the attribute blanket-suppresses Sendable checking for every AVFoundation type in these files — exactly where AVAudioPCMBuffer already crosses a Sendable-typed converter closure via the hand-justified InputBox. The next edit that captures an AVAudioConverter/AVAudioEngine in a task gets silence instead of a diagnostic. (Contrast: the two `@preconcurrency import WhisperKit` lines are STILL load-bearing — removing them yields 5 real 'sending non-Sendable AudioEncoding/TextDecoding/tokenizer' errors at WhisperKitProvider.swift:135 — leave those.)

FIX: Drop `@preconcurrency` from the two AVFoundation imports (Sources/M1K3Calls/StereoCallRecorder.swift:32, Sources/M1K3Calls/CallAudioWriter.swift:21). Keep both WhisperKit ones, ideally with a one-line comment noting they suppress real `sending` diagnostics on AudioStreamTranscriber's init (re-check on the next WhisperKit bump).

REVISED FIX: Proposed fix is correct as written: drop @preconcurrency from Sources/M1K3Calls/StereoCallRecorder.swift:32 and Sources/M1K3Calls/CallAudioWriter.swift:21; keep both M1K3WhisperKit ones with a one-line comment that they suppress real sending diagnostics on AudioStreamTranscriber construction (WhisperKitProvider.swift:135), re-check on the next WhisperKit bump. Two refinements: (a) land it as a ride-along in the next M1K3Calls-touching PR or nit-ledger batch, not its own PR; (b) if adding a comment, note the protected surface accurately — AVAudioPCMBuffer/AVAudioEngine are non-Sendable in SDK 26, while AVAudioConverter/AVAudioFormat are already Sendable, so the diagnostic value is specifically around buffer/engine captures.

VERIFIER: Independently reproduced every claim and extended the proof. (1) Both @preconcurrency AVFoundation imports are dead: with them removed, `swift build --target M1K3Calls` is clean AND a full -emit-sil compile of both files is diagnostic-free on macOS 26, iOS 26, and visionOS 26 SDKs — stronger than the auditor's macOS-only build, which matters because M1K3Calls compiles in the mobile-build CI job too (StereoCallRecorder's body is ScreenCaptureKit-gated but CallAudioWriter compiles everywhere). Note that -typecheck alone is NOT sufficient to prove this (sending diagnostics fire at SIL stage); the

---

## [34] NWPathMonitor handler→unordered Task trampoline can leave brainUpgradeNetworkPath stale; NWPathMonitor is an AsyncSequence since macOS 14 — the queue and trampoline are deletable
**M1K3App/AppEnvironment+BrainUpgrade.swift:341** · dim=modern-api-adoption · claimed=low → verified=low · effort=S

SCENARIO: pathUpdateHandler spawns a fresh unstructured `Task { @MainActor in self.brainUpgradeNetworkPath = path }` per update off a private DispatchQueue. Task creation order does not guarantee MainActor execution order, so two rapid path flips (Wi-Fi drops → hotspot engages) can land reversed: the stale 'unmetered Wi-Fi' path overwrites the current 'expensive hotspot' one and sticks until the next network event. OfferEligibility then reads isExpensive/isConstrained = false and raises the multi-GB brain-upgrade nudge on metered data — the exact case this guard exists to prevent (download still needs a tap, so impact is a wrongly-timed offer, not a silent download).

FIX: Replace the handler + label queue with one tracked MainActor task iterating the monitor: `brainUpgradePathTask = Task { @MainActor [weak self] in for await path in monitor { self?.brainUpgradeNetworkPath = path } }`; cancel it in stopBrainUpgradePathMonitor() alongside monitor.cancel(). Ordered delivery by construction, deletes the DispatchQueue and the per-update task spawn. (The other DispatchQueue in Sources — StereoCallRecorder.captureQueue — is API-required by SCStream.addStreamOutput(sampleHandlerQueue:) and should stay.)

REVISED FIX: The proposed fix is correct; three implementation details to include: (a) declare the tracked task next to the monitor — `@ObservationIgnored var brainUpgradePathTask: Task<Void, Never>?` at M1K3App/AppEnvironment.swift:~388; (b) do NOT call monitor.start(queue:) — iteration starts the monitor internally; keep storing the monitor so stopBrainUpgradePathMonitor() can `monitor.cancel()` (ends the sequence) AND `brainUpgradePathTask?.cancel(); brainUpgradePathTask = nil`; (c) per the repo's MurphySig convention, add a Review line to the file's signature block documenting the change (do not rewrite the signed 2026-07-03 block). Leave the `path?.isExpensive ?? false` nil-default as-is — first-turn-unknown-means-cheap is a pre-existing design choice, out of scope.

VERIFIER: VERIFIED against the actual file and SDK; could not refute the core claim.

(1) The code is exactly as quoted (AppEnvironment+BrainUpgrade.swift:341-345): pathUpdateHandler fires on a private serial DispatchQueue and spawns a fresh unstructured `Task { @MainActor in ... }` per update. Swift's runtime gives NO ordering guarantee between independently-created unstructured tasks: each is first scheduled on the global concurrent executor, and two pool threads can race to enqueue onto the MainActor (actors are priority-ordered, not FIFO). So a Wi-Fi-drop→hotspot burst (NWPathMonitor does emit rapid

---

## [35] ReviewModel's documented 'no deinit belt' rationale is obsolete — isolated deinit (Swift 6.1+, runtime present at the macOS 26 floor) makes the security-scoped-resource release free
**M1K3App/ReviewModel.swift:97** · dim=modern-api-adoption · claimed=low → verified=low · effort=S

SCENARIO: The comment explicitly declines a deinit cleanup because 'under Swift 6 a nonisolated deinit can't touch the MainActor-isolated scopedURL' and names the workarounds (nonisolated(unsafe)/assumeIsolated) as too costly — accepting a leaked stopAccessingSecurityScopedResource() as app-lifetime-safe. That tradeoff was priced pre-SE-0371. Today `isolated deinit` runs the deinit on the MainActor and touches scopedURL legally, so the belt costs one keyword. The latent hazard it guards: ReviewModel is app-lifetime NOW (AppEnvironment.swift:110), but the moment a second surface instantiates one (a per-window review panel, a menu-bar preview), each dropped instance strands a sandbox security-scope grant — kernel-side resource, invisible until file access starts failing.

FIX: Add `isolated deinit { releaseScopedAccess() }` to ReviewModel and rewrite the comment to say the belt is now free (SE-0371). One file, no behavior change on the current single-instance lifecycle; correct-by-construction if the lifecycle ever changes.

REVISED FIX: Proposed fix is correct and verified compilable: add `isolated deinit { releaseScopedAccess() }` and rewrite the lines 97-100 comment to state the belt is now free via SE-0371 isolated deinit. Two refinements: (a) the rewritten comment should say the original rationale OVERLOOKED isolated deinit (available since Swift 6.1), not that it predated it — don't fabricate history; (b) the file carries a MurphySig (Kev + claude-opus-4-8, 2026-06-19) — per house convention add a Review line documenting the change to the signed rationale. Ideally bundle with the next ReviewModel.swift touch rather than a standalone PR.

VERIFIER: Verified empirically on the actual toolchain. (1) The comment exists verbatim at ReviewModel.swift:97-100 and its original rationale is real: a plain nonisolated deinit touching scopedURL fails to compile on an identical @MainActor @Observable class shape ("main actor-isolated property 'scopedURL' can not be referenced from a nonisolated context") — @Observable's computed-property rewrite blocks even the Sendable-stored-property deinit exception. (2) The proposed fix is proven: `isolated deinit { releaseScopedAccess() }` compiles clean (swiftc -swift-version 6 -target arm64-apple-macos26, exit

---

