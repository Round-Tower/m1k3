# Core dependencies - Python 3.12+ compatible
requests>=2.32.4
tqdm>=4.66.4
huggingface-hub>=0.34.4

# AI/ML backends  
ctransformers>=0.2.27      # GGUF model support (ARM64 optimized)
torch>=2.2.2               # PyTorch for neural networks
torchaudio>=2.2.2          # Audio processing for torch
transformers>=4.55.3       # HuggingFace transformers (primary AI backend)

# Audio processing and Speech-to-Text (STT)
sounddevice>=0.4.6         # Audio input/output (required for all STT engines)

# STT Engines (in priority order for minimal footprint)
# 1. macOS Native (0MB) - requires: pip install pyobjc-framework-Speech pyobjc-framework-AVFoundation
# 2. Vosk (54MB) - lightweight offline STT with good accuracy
vosk>=0.3.45               # Lightweight offline speech recognition (primary choice)

# 3. Web Speech API (0MB) - fallback for cloud-based recognition
SpeechRecognition>=3.10.1  # Speech-to-text wrapper for multiple backends
pyaudio>=0.2.11            # Audio interface (required for SpeechRecognition)

# 4. Whisper (500MB+) - Optional high-quality STT (only if explicitly requested)
# openai-whisper>=20231117   # High-quality local speech recognition (disabled by default)
# Uncomment above line only if you need maximum accuracy and don't mind the large footprint

# Data and math
numpy>=1.26.4,<2.0         # Numerical computing (PyTorch compatibility requires 1.x)

# Disabled dependencies (Python 3.12+ incompatible)
# llama-cpp-python>=0.2.0  # Compilation fails - use ctransformers instead

# WebSocket and networking
websocket-server>=0.6.4    # WebSocket server for avatar dashboard
websocket-client>=1.8.0    # WebSocket client for CLI

# UI and display
rich>=13.7.1               # Rich terminal output
psutil>=5.9.0              # System monitoring  
textual>=5.3.0             # Modern TUI framework

# Voice synthesis enhancements - Python 3.12+ compatible alternatives
# TTS==0.22.0  # Disabled - not compatible with Python 3.12+
soundfile>=0.12.1
kittentts>=0.1.0
pyttsx3>=2.90    # Local TTS engine (works offline)
gTTS>=2.5.0      # Google Text-to-Speech (requires internet)

# VibeVoice - Microsoft's frontier open-source TTS (optional, requires CUDA GPU)
# Uncomment below for VibeVoice support (recommended for advanced users with NVIDIA GPU)
# diffusers>=0.21.0           # Required for VibeVoice diffusion models
# accelerate>=0.20.0          # GPU acceleration for transformers
# librosa>=0.10.0             # Audio processing utilities
# gradio>=4.0.0               # Optional: for VibeVoice demo interface

# Audio processing (optional)
scipy>=1.16.1

# Already listed above in networking section

# Additional dependencies
diskcache>=5.6.1           # Disk-based caching

# =============================================================================
# PYTHON 3.12+ COMPATIBILITY NOTES
# =============================================================================
# 
# ✅ WORKING: All dependencies above are tested and working on Python 3.12.7
# ❌ DISABLED: The following packages are not compatible with Python 3.12+
#    - TTS (Coqui TTS) 0.22.0: Requires Python <3.12
#    - llama-cpp-python: Compilation fails on current system (use ctransformers)
#
# 🔄 ALTERNATIVES PROVIDED:
#    - TTS → pyttsx3 (offline) + gTTS (online) + kittentts
#    - llama-cpp-python → ctransformers (GGUF support, ARM64 optimized)
#
# ⚠️  KNOWN DEPENDENCY CONFLICTS:
#    - NumPy 2.x: PyTorch requires NumPy <2.0, so we use numpy>=1.26.4,<2.0
#    - spaCy ecosystem: Complex dependency chain requires specific versions
#    - safety packages: May show pydantic version conflicts (non-critical)
#
# 📦 INSTALLATION: pip install -r requirements.txt
# 🧪 TESTING: python cli.py --help  (should load without NumPy warnings)