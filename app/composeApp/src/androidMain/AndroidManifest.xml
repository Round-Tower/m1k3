<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        間 AI - Privacy-First On-Device AI Assistant

        CRITICAL PRIVACY CONSTRAINT:
        This application MUST NOT request internet permission.
        All AI processing occurs 100% locally on device.

        Zero network transmission = Zero privacy risk.

        Permitted Permissions:
        - CAMERA: For multi-modal image analysis (Phase 4)
        - WRITE_EXTERNAL_STORAGE: For project file exports (Phase 4)
        - READ_EXTERNAL_STORAGE: For project file imports (Phase 4)

        FORBIDDEN Permissions (DO NOT ADD):
        - android.permission.INTERNET
        - android.permission.ACCESS_NETWORK_STATE
        - android.permission.ACCESS_WIFI_STATE
        - Any network-related permission
    -->

    <!-- Camera permission for multi-modal vision features -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Storage permissions for project file management -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Camera hardware feature (not required, but recommended) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>