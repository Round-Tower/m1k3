-- Migration 6 → 7: retire the bundled TriviaFact corpus.
--
-- Migration 5 introduced Source+Passage for user-imported notes. The
-- TriviaFact retrieval path was already dormant (RAG default = false,
-- 2026-04-19), its seeder infra (KnowledgeImportManager,
-- KnowledgeBaseImporter, 4 JSON KBs) is deleted in the same commit, and
-- the status-card chip is gone. Dropping the table reclaims storage from
-- any device that carried v2.x forward.
--
-- SQLite ON DELETE CASCADE from child rows is not a concern — nothing
-- references TriviaFact as a FK. Safe, one-way drop.
--
-- MurphySig: kev+claude / confidence 0.9 / 2026-04-20

DROP TABLE IF EXISTS TriviaFact;
