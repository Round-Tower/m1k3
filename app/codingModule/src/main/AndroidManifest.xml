<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution">

    <!--
        Dynamic Feature Module: Qwen2.5-Coder-0.5B
        Delivery Mode: On-Demand (user prompted before download)
        Size: ~130MB (120MB model + 10MB runtime)
    -->
    <dist:module
        dist:instant="false"
        dist:title="@string/coding_module_title">

        <!-- On-Demand Delivery: Downloaded when user requests code generation -->
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>

        <!-- Optional: Conditional delivery based on device capabilities -->
        <!-- Uncomment to require minimum 4GB RAM -->
        <!--
        <dist:fusing dist:include="true" />
        <dist:conditions>
            <dist:device-feature dist:name="android.hardware.ram.high" />
        </dist:conditions>
        -->
    </dist:module>

    <application>
        <!-- Module-specific resources and configuration -->
        <!-- No activities needed - this is a library module -->
    </application>
</manifest>
