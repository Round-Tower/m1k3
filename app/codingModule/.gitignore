# Build outputs
/build/
*.iml
.gradle/
local.properties

# Model files (too large for git)
src/main/assets/models/**/*.onnx
src/main/assets/models/**/*.ort

# Keep README files
!src/main/assets/models/**/README.md

# Tokenizer files (small enough to commit, but generated)
# Uncomment if you want to exclude these too:
# src/main/assets/models/**/*.json
# src/main/assets/models/**/tokenizer*
